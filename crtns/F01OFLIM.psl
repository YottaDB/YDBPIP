F01OFLIM(RecordUTBLOFILIM utblofilim, String vpar, Boolean vparNorm) // UTBLOFILIM - Officer Per Lending Limits Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (9)              03/02/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblofilim.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utblofilim,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utblofilim,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("OFFC").exists()!vx("KEY").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLOFILIM",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLOFILIM","OFFC = :utblofilim.offc and KEY = :utblofilim.key") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String OFFC
	type public String KEY

	type RecordUTBLOFILIM utblofilim = Db.getRecord("UTBLOFILIM","OFFC=:OFFC,KEY=:KEY")
	if (%ProcessMode = 2) do {
		do utblofilim.setMode(2)

		do F01OFLIM(utblofilim,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLOFILIM utblofilim

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLOFILIM","OFFC = :utblofilim.offc and KEY = :utblofilim.key") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblofilim,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblofilim,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblofilim)) S ^UTBL("OFILIM",vobj(utblofilim,-3),vobj(utblofilim,-4))=vobj(utblofilim)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLOFILIM utblofilim

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblofilim,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^UTBL("OFILIM",vobj(utblofilim,-3),vobj(utblofilim,-4))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordUTBLOFILIM utblofilim

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLOFILIM utblofilim

	if utblofilim.offc.isNull() do vreqerr("OFFC") quit
	if utblofilim.key.isNull() do vreqerr("KEY") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLOFILIM","MSG",1767,"UTBLOFILIM."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordUTBLOFILIM utblofilim
	if 'utblofilim.offc.isNull() set vfkey("^UTBL("_"""OFF"""_","_""""_utblofilim.offc_""""_")")="UTBLOFILIM(OFFC) -> UTBLOFF"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordUTBLOFILIM utblofilim
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("UTBLOFF",":utblofilim.offc") set vERRMSG = $$^MSG(8563,"UTBLOFILIM(OFFC) -> UTBLOFF") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLOFILIM utblofilim

	set X = utblofilim.offc if 'X.isNull(),'Db.isDefined("UTBLOFF","X") set vRM = $$^MSG(1485,X) do vdderr("OFFC", vRM) quit
	set X = utblofilim.key if 'X.isNull(),'Db.isDefined("STBLGRP","CLS=""L"",GRP=:X") set vRM = $$^MSG(1485,X) do vdderr("KEY", vRM) quit
	set X = utblofilim.apprlv if 'X.isNull(),'Db.isDefined("UTBLASTSLV","X") set vRM = $$^MSG(1485,X) do vdderr("APPRLV", vRM) quit
	set X = utblofilim.balplus if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"UTBLOFILIM.BALPLUS"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = utblofilim.lndlim if 'X.isNull(),'Db.isDefined("UTBLNDLIM","X") set vRM = $$^MSG(1485,X) do vdderr("LNDLIM", vRM) quit
	set X = utblofilim.ovrlims if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"UTBLOFILIM.OVRLIMS"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = utblofilim.ovrlimu if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"UTBLOFILIM.OVRLIMU"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = utblofilim.seclim if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"UTBLOFILIM.SECLIM"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = utblofilim.unslim if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"UTBLOFILIM.UNSLIM"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLOFILIM","MSG",979,"UTBLOFILIM."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLOFILIM utblofilim

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("OFFC").exists() set vux("OFFC") = vx("OFFC")
	if vx("KEY").exists() set vux("KEY") = vx("KEY")
	do vkey(1) set voldkey = utblofilim.offc_","_utblofilim.key // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = utblofilim.offc_","_utblofilim.key // Copy new keys into object
	type RecordUTBLOFILIM vnewrec = utblofilim.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLOFILIM",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordUTBLOFILIM utblofilim

	if vux("OFFC").exists() set utblofilim.offc = vux("OFFC").piece("|",i)
	if vux("KEY").exists() set utblofilim.key = vux("KEY").piece("|",i)
	quit
