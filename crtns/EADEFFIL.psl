EADEFFIL(RecordEVENTARGDEFS eventargdefs, String vpar, Boolean vparNorm) // EVENTARGDEFS - Event argument definitions Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:16 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (3)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = eventargdefs.getMode()                // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(eventargdefs,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(eventargdefs,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("TESTID").exists()!vx("ARGUMENTNUM").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("EVENTARGDEFS",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("EVENTARGDEFS","TESTID = :eventargdefs.testid and ARGUMENTNUM = :eventargdefs.argumentnum") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String TESTID
	type public String ARGUMENTNUM

	type RecordEVENTARGDEFS eventargdefs = Db.getRecord("EVENTARGDEFS","TESTID=:TESTID,ARGUMENTNUM=:ARGUMENTNUM")
	if (%ProcessMode = 2) do {
		do eventargdefs.setMode(2)

		do EADEFFIL(eventargdefs,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordEVENTARGDEFS eventargdefs

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("EVENTARGDEFS","TESTID = :eventargdefs.testid and ARGUMENTNUM = :eventargdefs.argumentnum") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(eventargdefs)) S ^EVENTINF("ARGDEFS",vobj(eventargdefs,-3),vobj(eventargdefs,-4))=vobj(eventargdefs)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordEVENTARGDEFS eventargdefs

	if vpar["/CASDEL/" do VCASDEL                          // Cascade delete

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^EVENTINF("ARGDEFS",vobj(eventargdefs,-3),vobj(eventargdefs,-4))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordEVENTARGDEFS eventargdefs

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordEVENTARGDEFS eventargdefs

	if eventargdefs.testid.isNull() do vreqerr("TESTID") quit
	if eventargdefs.argumentnum.isNull() do vreqerr("ARGUMENTNUM") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EVENTARGDEFS","MSG",1767,"EVENTARGDEFS."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordEVENTARGDEFS eventargdefs
	if 'eventargdefs.testid.isNull() set vfkey("^EVENTINF("_"""TESTS"""_","_""""_eventargdefs.testid_""""_")")="EVENTARGDEFS(TESTID) -> EVENTTESTS"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordEVENTARGDEFS eventargdefs
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("EVENTTESTS",":eventargdefs.testid") set vERRMSG = $$^MSG(8563,"EVENTARGDEFS(TESTID) -> EVENTTESTS") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordEVENTARGDEFS eventargdefs

	set X = eventargdefs.testid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TESTID", vRM) quit
	set X = eventargdefs.argumentnum if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("ARGUMENTNUM", vRM) quit
	if eventargdefs.description.length()>40 set vRM = $$^MSG(1076,40) do vdderr("DESCRIPTION", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EVENTARGDEFS","MSG",979,"EVENTARGDEFS."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordEVENTARGDEFS eventargdefs

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("TESTID").exists() set vux("TESTID") = vx("TESTID")
	if vx("ARGUMENTNUM").exists() set vux("ARGUMENTNUM") = vx("ARGUMENTNUM")
	do vkey(1) set voldkey = eventargdefs.testid_","_eventargdefs.argumentnum // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = eventargdefs.testid_","_eventargdefs.argumentnum // Copy new keys into object
	type RecordEVENTARGDEFS vnewrec = eventargdefs.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("EVENTARGDEFS",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordEVENTARGDEFS eventargdefs

	if vux("TESTID").exists() set eventargdefs.testid = vux("TESTID").piece("|",i)
	if vux("ARGUMENTNUM").exists() set eventargdefs.argumentnum = vux("ARGUMENTNUM").piece("|",i)
	quit

VCASDEL // Cascade delete logic

	type public RecordEVENTARGDEFS eventargdefs
	type public String vpar

	do Db.delete("EVENTARGVALS","TESTID=:eventargdefs.testid AND ARGUMENTNUM=:eventargdefs.argumentnum",vpar) // Cascade delete

	do Db.delete("EVENTSYSARGV","TESTID=:eventargdefs.testid AND ARGUMENTNUM=:eventargdefs.argumentnum",vpar) // Cascade delete

	quit
