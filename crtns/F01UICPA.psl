F01UICPA(RecordUTBLICPA utblicpa, String vpar, Boolean vparNorm) // UTBLICPA - Int Calculation Principal Adjustment Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (16)             03/30/2006
	// Trigger Definition (1)                      12/17/2004
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblicpa.getMode()                    // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utblicpa,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utblicpa,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	// Define local variables for access keys for legacy triggers
	type String KEY = utblicpa.key

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/TRIGBEF/" do VBI                            // Before insert triggers
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("KEY").exists() do vkchged quit                 // Primary key changed
		if vpar["/TRIGBEF/" do VBU                            // Before update triggers
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLICPA",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLICPA","KEY = :utblicpa.key")  // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String KEY

	type RecordUTBLICPA utblicpa = Db.getRecord("UTBLICPA","KEY=:KEY")
	if (%ProcessMode = 2) do {
		do utblicpa.setMode(2)

		do F01UICPA(utblicpa,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLICPA utblicpa

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLICPA","KEY = :utblicpa.key") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblicpa,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblicpa,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblicpa)) S ^UTBL("ICPA",vobj(utblicpa,-3))=vobj(utblicpa)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLICPA utblicpa

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblicpa,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^UTBL("ICPA",vobj(utblicpa,-3))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordUTBLICPA utblicpa

	// Type local variables for access keys for defaults
	type public String KEY

	if utblicpa.pc1.isNull() set utblicpa.pc1 = 0          // pc1
	if utblicpa.pc10.isNull() set utblicpa.pc10 = 0        // pc10
	if utblicpa.pc11.isNull() set utblicpa.pc11 = 0        // pc11
	if utblicpa.pc12.isNull() set utblicpa.pc12 = 0        // pc12
	if utblicpa.pc13.isNull() set utblicpa.pc13 = 0        // pc13
	if utblicpa.pc2.isNull() set utblicpa.pc2 = 0          // pc2
	if utblicpa.pc3.isNull() set utblicpa.pc3 = 0          // pc3
	if utblicpa.pc4.isNull() set utblicpa.pc4 = 0          // pc4
	if utblicpa.pc5.isNull() set utblicpa.pc5 = 0          // pc5
	if utblicpa.pc6.isNull() set utblicpa.pc6 = 0          // pc6
	if utblicpa.pc7.isNull() set utblicpa.pc7 = 0          // pc7
	if utblicpa.pc8.isNull() set utblicpa.pc8 = 0          // pc8
	if utblicpa.pc9.isNull() set utblicpa.pc9 = 0          // pc9
	quit


vreqn // Validate required data items

	type public RecordUTBLICPA utblicpa

	if utblicpa.desc.isNull() do vreqerr("DESC") quit
	if utblicpa.pc1.isNull() do vreqerr("PC1") quit
	if utblicpa.pc10.isNull() do vreqerr("PC10") quit
	if utblicpa.pc11.isNull() do vreqerr("PC11") quit
	if utblicpa.pc12.isNull() do vreqerr("PC12") quit
	if utblicpa.pc13.isNull() do vreqerr("PC13") quit
	if utblicpa.pc2.isNull() do vreqerr("PC2") quit
	if utblicpa.pc3.isNull() do vreqerr("PC3") quit
	if utblicpa.pc4.isNull() do vreqerr("PC4") quit
	if utblicpa.pc5.isNull() do vreqerr("PC5") quit
	if utblicpa.pc6.isNull() do vreqerr("PC6") quit
	if utblicpa.pc7.isNull() do vreqerr("PC7") quit
	if utblicpa.pc8.isNull() do vreqerr("PC8") quit
	if utblicpa.pc9.isNull() do vreqerr("PC9") quit
	if utblicpa.prio.isNull() do vreqerr("PRIO") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLICPA utblicpa

	if utblicpa.key.isNull() do vreqerr("KEY") quit

		if vx("DESC").exists(),utblicpa.desc.isNull() do vreqerr("DESC") quit
		if vx("PC1").exists(),utblicpa.pc1.isNull() do vreqerr("PC1") quit
		if vx("PC10").exists(),utblicpa.pc10.isNull() do vreqerr("PC10") quit
		if vx("PC11").exists(),utblicpa.pc11.isNull() do vreqerr("PC11") quit
		if vx("PC12").exists(),utblicpa.pc12.isNull() do vreqerr("PC12") quit
		if vx("PC13").exists(),utblicpa.pc13.isNull() do vreqerr("PC13") quit
		if vx("PC2").exists(),utblicpa.pc2.isNull() do vreqerr("PC2") quit
		if vx("PC3").exists(),utblicpa.pc3.isNull() do vreqerr("PC3") quit
		if vx("PC4").exists(),utblicpa.pc4.isNull() do vreqerr("PC4") quit
		if vx("PC5").exists(),utblicpa.pc5.isNull() do vreqerr("PC5") quit
		if vx("PC6").exists(),utblicpa.pc6.isNull() do vreqerr("PC6") quit
		if vx("PC7").exists(),utblicpa.pc7.isNull() do vreqerr("PC7") quit
		if vx("PC8").exists(),utblicpa.pc8.isNull() do vreqerr("PC8") quit
		if vx("PC9").exists(),utblicpa.pc9.isNull() do vreqerr("PC9") quit
		if vx("PRIO").exists(),utblicpa.prio.isNull() do vreqerr("PRIO") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLICPA","MSG",1767,"UTBLICPA."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VBI //
	type public Number ER = 0
	type public String vx(),RM
	do vbi1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit

VBU //
	type public Number ER = 0
	type public String vx(),RM
	type public RecordUTBLICPA utblicpa
	do vbu1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	do AUDIT^UCUTILN(utblicpa,.vx(),1,"|")
	quit


vbi1 // Trigger BEFORE_I_U - Before insert and update

	do vbu1

	quit

vbu1 // Trigger BEFORE_I_U - Before insert and update

	type public RecordUTBLICPA utblicpa
	do utblicpa.setAuditFlag(1)
	/*
	----Revision History---------------------------------------------------
		
	10/13/02 - GRAY - 49794
		   Moved code from ICPA^LNSCDRV into this trigger def.

	*/

	 
								/*
								Sets up ^UTBL("ICPA",SEQ)=Description|Action pieces
	
								Interest Calculation Principal Adjustment
								*/
	
								new DI,PRIO
								set PRIO=""
	
								// Description
								if '$L(utblicpa.desc) quit
	
								for I=1:1:13 do {
																set DI="PC"_I
																set utblicpa.@DI=+utblicpa.@DI
																set PRIO=PRIO_utblicpa.@DI
																}
	
								set utblicpa.prio=PRIO
	
								quit


vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLICPA utblicpa

	if utblicpa.key.length()>12 set vRM = $$^MSG(1076,12) do vdderr("KEY", vRM) quit
	if utblicpa.desc.length()>30 set vRM = $$^MSG(1076,30) do vdderr("DESC", vRM) quit
	if '("01"[utblicpa.PC1) set vRM=$$^MSG(742,"L") do vdderr("PC1", vRM) quit
	if '("01"[utblicpa.PC10) set vRM=$$^MSG(742,"L") do vdderr("PC10", vRM) quit
	if '("01"[utblicpa.PC11) set vRM=$$^MSG(742,"L") do vdderr("PC11", vRM) quit
	if '("01"[utblicpa.PC12) set vRM=$$^MSG(742,"L") do vdderr("PC12", vRM) quit
	if '("01"[utblicpa.PC13) set vRM=$$^MSG(742,"L") do vdderr("PC13", vRM) quit
	if '("01"[utblicpa.PC2) set vRM=$$^MSG(742,"L") do vdderr("PC2", vRM) quit
	if '("01"[utblicpa.PC3) set vRM=$$^MSG(742,"L") do vdderr("PC3", vRM) quit
	if '("01"[utblicpa.PC4) set vRM=$$^MSG(742,"L") do vdderr("PC4", vRM) quit
	if '("01"[utblicpa.PC5) set vRM=$$^MSG(742,"L") do vdderr("PC5", vRM) quit
	if '("01"[utblicpa.PC6) set vRM=$$^MSG(742,"L") do vdderr("PC6", vRM) quit
	if '("01"[utblicpa.PC7) set vRM=$$^MSG(742,"L") do vdderr("PC7", vRM) quit
	if '("01"[utblicpa.PC8) set vRM=$$^MSG(742,"L") do vdderr("PC8", vRM) quit
	if '("01"[utblicpa.PC9) set vRM=$$^MSG(742,"L") do vdderr("PC9", vRM) quit
	if utblicpa.prio.length()>16 set vRM = $$^MSG(1076,16) do vdderr("PRIO", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLICPA","MSG",979,"UTBLICPA."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLICPA utblicpa

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	set vux = vx("KEY")
	set voldkey = vux.piece("|",1),vobj(utblicpa,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/TRIGBEF/" do VBU
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(utblicpa,-3) = vnewkey // Copy new keys into object
	type RecordUTBLICPA vnewrec = utblicpa.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLICPA",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	set vobj(utblicpa,-3) = vux.piece("|",1)               // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit
