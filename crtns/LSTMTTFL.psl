LSTMTTFL(RecordLETSTMTTOT letstmttot, String vpar, Boolean vparNorm) // LETSTMTTOT - Statement Extract Totals Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:16 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (12)             04/13/2007
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = letstmttot.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(letstmttot,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(letstmttot,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("SJD").exists()!vx("EXTRACT").exists()!vx("CO").exists()!vx("FORM").exists()!vx("VAR").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("LETSTMTTOT",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("LETSTMTTOT","SJD = :letstmttot.sjd and EXTRACT = :letstmttot.extract and CO = :letstmttot.co and FORM = :letstmttot.form and VAR = :letstmttot.var") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String SJD
	type public String EXTRACT
	type public String CO
	type public String FORM
	type public String VAR

	type RecordLETSTMTTOT letstmttot = Db.getRecord("LETSTMTTOT","SJD=:SJD,EXTRACT=:EXTRACT,CO=:CO,FORM=:FORM,VAR=:VAR")
	if (%ProcessMode = 2) do {
		do letstmttot.setMode(2)

		do LSTMTTFL(letstmttot,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordLETSTMTTOT letstmttot

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("LETSTMTTOT","SJD = :letstmttot.sjd and EXTRACT = :letstmttot.extract and CO = :letstmttot.co and FORM = :letstmttot.form and VAR = :letstmttot.var") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(letstmttot)) S ^LETSTMTT(vobj(letstmttot,-3),vobj(letstmttot,-4),vobj(letstmttot,-5),vobj(letstmttot,-6),vobj(letstmttot,-7))=vobj(letstmttot)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordLETSTMTTOT letstmttot


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^LETSTMTT(vobj(letstmttot,-3),vobj(letstmttot,-4),vobj(letstmttot,-5),vobj(letstmttot,-6),vobj(letstmttot,-7))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordLETSTMTTOT letstmttot

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordLETSTMTTOT letstmttot

	if letstmttot.sjd.isNull() do vreqerr("SJD") quit
	if letstmttot.extract.isNull() do vreqerr("EXTRACT") quit
	if letstmttot.co.isNull() do vreqerr("CO") quit
	if letstmttot.form.isNull() do vreqerr("FORM") quit
	if letstmttot.var.isNull() do vreqerr("VAR") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("LETSTMTTOT","MSG",1767,"LETSTMTTOT."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordLETSTMTTOT letstmttot

	set X = letstmttot.sjd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("SJD", vRM) quit
	if letstmttot.extract.length()>70 set vRM = $$^MSG(1076,70) do vdderr("EXTRACT", vRM) quit
	set X = letstmttot.co if 'X.isNull(),'Db.isDefined("UTBLCO","X") set vRM = $$^MSG(1485,X) do vdderr("CO", vRM) quit
	if letstmttot.form.length()>12 set vRM = $$^MSG(1076,12) do vdderr("FORM", vRM) quit
	set X = letstmttot.var if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("VAR", vRM) quit
	set X = letstmttot.dat if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("DAT", vRM) quit
	set X = letstmttot.tim if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"C") do vdderr("TIM", vRM) quit
	set X = letstmttot.totbill if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"LETSTMTTOT.TOTBILL"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = letstmttot.totcid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TOTCID", vRM) quit
	set X = letstmttot.totrecs if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TOTRECS", vRM) quit
	set X = letstmttot.totstm if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TOTSTM", vRM) quit
	set X = letstmttot.tottrn if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TOTTRN", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("LETSTMTTOT","MSG",979,"LETSTMTTOT."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordLETSTMTTOT letstmttot

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("SJD").exists() set vux("SJD") = vx("SJD")
	if vx("EXTRACT").exists() set vux("EXTRACT") = vx("EXTRACT")
	if vx("CO").exists() set vux("CO") = vx("CO")
	if vx("FORM").exists() set vux("FORM") = vx("FORM")
	if vx("VAR").exists() set vux("VAR") = vx("VAR")
	do vkey(1) set voldkey = letstmttot.sjd_","_letstmttot.extract_","_letstmttot.co_","_letstmttot.form_","_letstmttot.var // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = letstmttot.sjd_","_letstmttot.extract_","_letstmttot.co_","_letstmttot.form_","_letstmttot.var // Copy new keys into object
	type RecordLETSTMTTOT vnewrec = letstmttot.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("LETSTMTTOT",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordLETSTMTTOT letstmttot

	if vux("SJD").exists() set letstmttot.sjd = vux("SJD").piece("|",i)
	if vux("EXTRACT").exists() set letstmttot.extract = vux("EXTRACT").piece("|",i)
	if vux("CO").exists() set letstmttot.co = vux("CO").piece("|",i)
	if vux("FORM").exists() set letstmttot.form = vux("FORM").piece("|",i)
	if vux("VAR").exists() set letstmttot.var = vux("VAR").piece("|",i)
	quit
