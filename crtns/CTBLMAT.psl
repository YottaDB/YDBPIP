CTBLMAT(RecordCTBLMATATT ctblmatatt, String vpar, Boolean vparNorm) // CTBLMATATT - Interest/Dividend Matrix Attributes Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:14 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (5)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = ctblmatatt.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(ctblmatatt,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(ctblmatatt,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("MATATT").exists() do vkchged quit              // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("CTBLMATATT",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("CTBLMATATT","MATATT = :ctblmatatt.matatt") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String MATATT

	type RecordCTBLMATATT ctblmatatt = Db.getRecord("CTBLMATATT","MATATT=:MATATT")
	if (%ProcessMode = 2) do {
		do ctblmatatt.setMode(2)

		do CTBLMAT(ctblmatatt,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordCTBLMATATT ctblmatatt

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("CTBLMATATT","MATATT = :ctblmatatt.matatt") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(ctblmatatt,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(ctblmatatt,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(ctblmatatt)) S ^CTBL("MATATT",vobj(ctblmatatt,-3))=vobj(ctblmatatt)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordCTBLMATATT ctblmatatt

	if vpar'["/NOLOG/" do ^DBSLOGIT(ctblmatatt,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^CTBL("MATATT",vobj(ctblmatatt,-3))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordCTBLMATATT ctblmatatt

	// Type local variables for access keys for defaults
	type public String MATATT

	if ctblmatatt.valcol.isNull() set ctblmatatt.valcol = 0 // valcol
	if ctblmatatt.valrow.isNull() set ctblmatatt.valrow = 0 // valrow
	quit


vreqn // Validate required data items

	type public RecordCTBLMATATT ctblmatatt

	if ctblmatatt.desc.isNull() do vreqerr("DESC") quit
	if ctblmatatt.valcol.isNull() do vreqerr("VALCOL") quit
	if ctblmatatt.valrow.isNull() do vreqerr("VALROW") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordCTBLMATATT ctblmatatt

	if ctblmatatt.matatt.isNull() do vreqerr("MATATT") quit

		if vx("DESC").exists(),ctblmatatt.desc.isNull() do vreqerr("DESC") quit
		if vx("VALCOL").exists(),ctblmatatt.valcol.isNull() do vreqerr("VALCOL") quit
		if vx("VALROW").exists(),ctblmatatt.valrow.isNull() do vreqerr("VALROW") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CTBLMATATT","MSG",1767,"CTBLMATATT."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordCTBLMATATT ctblmatatt

	set X = ctblmatatt.matatt if 'X.isNull(),'Db.isDefined("STBLMATATT","X") set vRM = $$^MSG(1485,X) do vdderr("MATATT", vRM) quit
	if ctblmatatt.desc.length()>50 set vRM = $$^MSG(1076,50) do vdderr("DESC", vRM) quit
	if ctblmatatt.userdesc.length()>50 set vRM = $$^MSG(1076,50) do vdderr("USERDESC", vRM) quit
	if '("01"[ctblmatatt.VALCOL) set vRM=$$^MSG(742,"L") do vdderr("VALCOL", vRM) quit
	if '("01"[ctblmatatt.VALROW) set vRM=$$^MSG(742,"L") do vdderr("VALROW", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CTBLMATATT","MSG",979,"CTBLMATATT."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordCTBLMATATT ctblmatatt

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	set vux = vx("MATATT")
	set voldkey = vux.piece("|",1),vobj(ctblmatatt,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(ctblmatatt,-3) = vnewkey // Copy new keys into object
	type RecordCTBLMATATT vnewrec = ctblmatatt.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("CTBLMATATT",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	set vobj(ctblmatatt,-3) = vux.piece("|",1)             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit
