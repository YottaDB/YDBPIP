SKLBFMFL(RecordDAYENDBFM dayendbfm, String vpar, Boolean vparNorm) // DAYENDBFM - Batch file maintenance data Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:14 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (11)             01/12/2005
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = dayendbfm.getMode()                   // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(dayendbfm,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(dayendbfm,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("TJD").exists()!vx("CLS").exists()!vx("TYPE").exists()!vx("CHGNUM").exists()!vx("SEQ").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("DAYENDBFM",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("DAYENDBFM","TJD = :dayendbfm.tjd and CLS = :dayendbfm.cls and TYPE = :dayendbfm.type and CHGNUM = :dayendbfm.chgnum and SEQ = :dayendbfm.seq") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String TJD
	type public String CLS
	type public String TYPE
	type public String CHGNUM
	type public String SEQ

	type RecordDAYENDBFM dayendbfm = Db.getRecord("DAYENDBFM","TJD=:TJD,CLS=:CLS,TYPE=:TYPE,CHGNUM=:CHGNUM,SEQ=:SEQ")
	if (%ProcessMode = 2) do {
		do dayendbfm.setMode(2)

		do SKLBFMFL(dayendbfm,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordDAYENDBFM dayendbfm

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("DAYENDBFM","TJD = :dayendbfm.tjd and CLS = :dayendbfm.cls and TYPE = :dayendbfm.type and CHGNUM = :dayendbfm.chgnum and SEQ = :dayendbfm.seq") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(dayendbfm)) K:$D(vobj(dayendbfm,1,1)) ^DAYEND(vobj(dayendbfm,-3),"BFM",vobj(dayendbfm,-4),vobj(dayendbfm,-5),vobj(dayendbfm,-6),vobj(dayendbfm,-7)) S ^DAYEND(vobj(dayendbfm,-3),"BFM",vobj(dayendbfm,-4),vobj(dayendbfm,-5),vobj(dayendbfm,-6),vobj(dayendbfm,-7))=vobj(dayendbfm)
		#ENDBYPASS
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
			if $D(vobj(dayendbfm,1,1)) N vS1,vS2 S vS1=0 F vS2=1:450:$L(vobj(dayendbfm,1,1)) S vS1=vS1+1,^DAYEND(vobj(dayendbfm,-3),"BFM",vobj(dayendbfm,-4),vobj(dayendbfm,-5),vobj(dayendbfm,-6),vobj(dayendbfm,-7),vS1)=$E(vobj(dayendbfm,1,1),vS2,vS2+449)
			#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordDAYENDBFM dayendbfm


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^DAYEND(vobj(dayendbfm,-3),"BFM",vobj(dayendbfm,-4),vobj(dayendbfm,-5),vobj(dayendbfm,-6),vobj(dayendbfm,-7))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordDAYENDBFM dayendbfm

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordDAYENDBFM dayendbfm

	if dayendbfm.tjd.isNull() do vreqerr("TJD") quit
	if dayendbfm.cls.isNull() do vreqerr("CLS") quit
	if dayendbfm.type.isNull() do vreqerr("TYPE") quit
	if dayendbfm.chgnum.isNull() do vreqerr("CHGNUM") quit
	if dayendbfm.seq.isNull() do vreqerr("SEQ") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DAYENDBFM","MSG",1767,"DAYENDBFM."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordDAYENDBFM dayendbfm

	set X = dayendbfm.tjd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("TJD", vRM) quit
	if dayendbfm.cls.length()>1 set vRM = $$^MSG(1076,1) do vdderr("CLS", vRM) quit
	set X = dayendbfm.type if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TYPE", vRM) quit
	set X = dayendbfm.chgnum if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("CHGNUM", vRM) quit
	set X = dayendbfm.seq if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("SEQ", vRM) quit
	if dayendbfm.col.length()>256 set vRM = $$^MSG(1076,256) do vdderr("COL", vRM) quit
	set X = dayendbfm.lastrec if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("LASTREC", vRM) quit
	if dayendbfm.newvalue.length()>256 set vRM = $$^MSG(1076,256) do vdderr("NEWVALUE", vRM) quit
	if dayendbfm.tbl.length()>256 set vRM = $$^MSG(1076,256) do vdderr("TBL", vRM) quit
	if dayendbfm.tlo.length()>40 set vRM = $$^MSG(1076,40) do vdderr("TLO", vRM) quit
	if dayendbfm.uid.length()>20 set vRM = $$^MSG(1076,20) do vdderr("UID", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DAYENDBFM","MSG",979,"DAYENDBFM."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordDAYENDBFM dayendbfm

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("TJD").exists() set vux("TJD") = vx("TJD")
	if vx("CLS").exists() set vux("CLS") = vx("CLS")
	if vx("TYPE").exists() set vux("TYPE") = vx("TYPE")
	if vx("CHGNUM").exists() set vux("CHGNUM") = vx("CHGNUM")
	if vx("SEQ").exists() set vux("SEQ") = vx("SEQ")
	do vkey(1) set voldkey = dayendbfm.tjd_","_dayendbfm.cls_","_dayendbfm.type_","_dayendbfm.chgnum_","_dayendbfm.seq // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = dayendbfm.tjd_","_dayendbfm.cls_","_dayendbfm.type_","_dayendbfm.chgnum_","_dayendbfm.seq // Copy new keys into object
	type RecordDAYENDBFM vnewrec = dayendbfm.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("DAYENDBFM",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordDAYENDBFM dayendbfm

	if vux("TJD").exists() set dayendbfm.tjd = vux("TJD").piece("|",i)
	if vux("CLS").exists() set dayendbfm.cls = vux("CLS").piece("|",i)
	if vux("TYPE").exists() set dayendbfm.type = vux("TYPE").piece("|",i)
	if vux("CHGNUM").exists() set dayendbfm.chgnum = vux("CHGNUM").piece("|",i)
	if vux("SEQ").exists() set dayendbfm.seq = vux("SEQ").piece("|",i)
	quit
