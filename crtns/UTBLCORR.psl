UTBLCORR(RecordUTBLCORRE utblcorre, String vpar, Boolean vparNorm) // UTBLCORRE - Correspondent Institutions Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (26)             10/11/2006
	// Trigger Definition (2)                      09/21/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblcorre.getMode()                   // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utblcorre,.vxins(),11,"|")
	if %O = 1 quit:'utblcorre.isChanged()  do AUDIT^UCUTILN(utblcorre,.vx(),11,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	// Define local variables for access keys for legacy triggers
	type String KEY = utblcorre.key

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
		if vpar["/TRIGAFT/" do VAI                            // After insert triggers
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("KEY").exists() do vkchged quit                 // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLCORRE",.vx)
		set %O = 1 do vexec
		if vpar["/TRIGAFT/" do VAU                            // After update triggers
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
		if vpar["/TRIGAFT/" do VAI                            // After insert triggers
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLCORRE","KEY = :utblcorre.key") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String KEY

	type RecordUTBLCORRE utblcorre = Db.getRecord("UTBLCORRE","KEY=:KEY")
	if (%ProcessMode = 2) do {
		do utblcorre.setMode(2)

		do UTBLCORR(utblcorre,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLCORRE utblcorre

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLCORRE","KEY = :utblcorre.key") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblcorre,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblcorre,%O,.vx())

		type String n = -1
		type String x

		if %O = 0 for  set n = vobj(utblcorre,n).order() quit:n.isNull()  do {
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^UTBL("CORRE",vobj(utblcorre,-3),n)=vobj(utblcorre,n)
			#ENDBYPASS
		}

		else  for  set n = vobj(utblcorre,-100,n).order() quit:n.isNull()  do {
			quit:'$D(vobj(utblcorre,n))
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^UTBL("CORRE",vobj(utblcorre,-3),n)=vobj(utblcorre,n)
			#ENDBYPASS
		}

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblcorre)) S ^UTBL("CORRE",vobj(utblcorre,-3))=vobj(utblcorre)
		#ENDBYPASS
	}

	quit

vload // Record Load - force loading of unloaded data

	type public RecordUTBLCORRE utblcorre
	type String n = ""

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	for  set n=$order(^UTBL("CORRE",vobj(utblcorre,-3),n)) quit:n=""  if '$D(vobj(utblcorre,n)),$D(^UTBL("CORRE",vobj(utblcorre,-3),n))#2 set vobj(utblcorre,n)=^(n)
	#ENDBYPASS
	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLCORRE utblcorre

	if 'vkeychg.get(),utblcorre.isChanged() throw Class.new("Error","%PSL-E-DBFILER,Deleted object cannot be modified")

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblcorre,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^UTBL("CORRE",vobj(utblcorre,-3))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordUTBLCORRE utblcorre

	// Type local variables for access keys for defaults
	type public String KEY

	if utblcorre.auth.isNull() set utblcorre.auth = 0      // auth
	if utblcorre.loaded.isNull() set utblcorre.loaded = 0  // loaded
	if utblcorre.ourbr.isNull() set utblcorre.ourbr = 0    // ourbr
	quit


vreqn // Validate required data items

	type public RecordUTBLCORRE utblcorre

	if utblcorre.auth.isNull() do vreqerr("AUTH") quit
	if utblcorre.loaded.isNull() do vreqerr("LOADED") quit
	if utblcorre.name.isNull() do vreqerr("NAME") quit
	if utblcorre.ourbr.isNull() do vreqerr("OURBR") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLCORRE utblcorre

	if utblcorre.key.isNull() do vreqerr("KEY") quit

	if 'vobj(utblcorre,-100,"0*","").order().isNull() do {
		if vx("NAME").exists(),utblcorre.name.isNull() do vreqerr("NAME") quit
		if vx("OURBR").exists(),utblcorre.ourbr.isNull() do vreqerr("OURBR") quit
		if vx("AUTH").exists(),utblcorre.auth.isNull() do vreqerr("AUTH") quit
		if vx("LOADED").exists(),utblcorre.loaded.isNull() do vreqerr("LOADED") quit
	}
		if vx("AUTH").exists(),utblcorre.auth.isNull() do vreqerr("AUTH") quit
		if vx("LOADED").exists(),utblcorre.loaded.isNull() do vreqerr("LOADED") quit
		if vx("NAME").exists(),utblcorre.name.isNull() do vreqerr("NAME") quit
		if vx("OURBR").exists(),utblcorre.ourbr.isNull() do vreqerr("OURBR") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLCORRE","MSG",1767,"UTBLCORRE."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VAI //
	type public Number ER = 0
	type public String vx(),RM
	do vai1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit

VAU //
	type public Number ER = 0
	type public String vx(),RM
	if vx("").order().isNull() quit
	if vx("MZIP").exists() do vau1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vai1 // Trigger AFTER_INSERT - After Insert

	type public RecordUTBLCORRE utblcorre

	/*
	-- Revision History -----------------------------------------------

	08/21/06 - KumarSS - 22169
		   Initial Revision.
		   This trigger validates ZIP code column.
	-----------------------------------------------------------------------------
	*/

	type public Boolean ER
	type public String RM

	// Validate ZIP code - Correspondent Institutions
	do ZIP^PROCZIP(utblcorre.cntry, utblcorre.state, utblcorre.mzip)
	if ER.get() do Runtime.setErrMSG("UTBLCORRE", 3333, RM)

	quit
	

vau1 // Trigger AU_MZIP - After Update of Zip Code

	type public RecordUTBLCORRE utblcorre

	/*
	--Revision History-----------------------------------------------------------

	08/21/06 - KumarSS - 22169
		   Initial Revision.
		   This trigger validates ZIP code column.
	-----------------------------------------------------------------------------
	*/

	type public Boolean ER
	type public String RM

	// Validate ZIP code - Correspondent Institutions
	do ZIP^PROCZIP(utblcorre.cntry, utblcorre.state, utblcorre.mzip)
	if ER.get() do Runtime.setErrMSG("UTBLCORRE", 3333, RM)

	quit


vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLCORRE utblcorre

	if (%O = 2) do vload
	if utblcorre.key.length()>11 set vRM = $$^MSG(1076,11) do vdderr("KEY", vRM) quit

	if vobj(utblcorre).exists() ! 'vobj(utblcorre,"").order().isNull() do {

		set X = utblcorre.acn if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("ACN", vRM) quit
		if utblcorre.addr1.length()>35 set vRM = $$^MSG(1076,35) do vdderr("ADDR1", vRM) quit
		if utblcorre.addr2.length()>35 set vRM = $$^MSG(1076,35) do vdderr("ADDR2", vRM) quit
		if '("01"[utblcorre.AUTH) set vRM=$$^MSG(742,"L") do vdderr("AUTH", vRM) quit
		if utblcorre.bic.length()>11 set vRM = $$^MSG(1076,11) do vdderr("BIC", vRM) quit
		set X = utblcorre.bl if 'X.isNull(),X'?1.8N,X'?1"-"1.7N set vRM=$$^MSG(742,"N") do vdderr("BL", vRM) quit
		if utblcorre.brinfo2.length()>35 set vRM = $$^MSG(1076,35) do vdderr("BRINFO2", vRM) quit
		set X = utblcorre.cc if 'X.isNull(),X'?1.9N,X'?1"-"1.8N set vRM=$$^MSG(742,"N") do vdderr("CC", vRM) quit
		if utblcorre.city.length()>40 set vRM = $$^MSG(1076,40) do vdderr("CITY", vRM) quit
		set X = utblcorre.cntry if 'X.isNull(),'Db.isDefined("STBLCNTRY","X") set vRM = $$^MSG(1485,X) do vdderr("CNTRY", vRM) quit
		set X = utblcorre.cp if 'X.isNull(),X'?1.3N,X'?1"-"1.2N set vRM=$$^MSG(742,"N") do vdderr("CP", vRM) quit
		set X = utblcorre.cu if 'X.isNull(),X'?1.6N,X'?1"-"1.5N set vRM=$$^MSG(742,"N") do vdderr("CU", vRM) quit
		set X = utblcorre.fw if 'X.isNull(),X'?1.9N,X'?1"-"1.8N set vRM=$$^MSG(742,"N") do vdderr("FW", vRM) quit
		set X = utblcorre.instcd if 'X.isNull(),'Db.isDefined("UTBLEFTINST","X") set vRM = $$^MSG(1485,X) do vdderr("INSTCD", vRM) quit
		if utblcorre.instcd2.length()>35 set vRM = $$^MSG(1076,35) do vdderr("INSTCD2", vRM) quit
		if '("01"[utblcorre.LOADED) set vRM=$$^MSG(742,"L") do vdderr("LOADED", vRM) quit
		if utblcorre.mzip.length()>10 set vRM = $$^MSG(1076,10) do vdderr("MZIP", vRM) quit
		if utblcorre.name.length()>35 set vRM = $$^MSG(1076,35) do vdderr("NAME", vRM) quit
		if utblcorre.name2.length()>35 set vRM = $$^MSG(1076,35) do vdderr("NAME2", vRM) quit
		if utblcorre.name3.length()>35 set vRM = $$^MSG(1076,35) do vdderr("NAME3", vRM) quit
		if '("01"[utblcorre.OURBR) set vRM=$$^MSG(742,"L") do vdderr("OURBR", vRM) quit
		if utblcorre.ourbrnam.length()>35 set vRM = $$^MSG(1076,35) do vdderr("OURBRNAM", vRM) quit
		set X = utblcorre.sc if 'X.isNull(),X'?1.6N,X'?1"-"1.5N set vRM=$$^MSG(742,"N") do vdderr("SC", vRM) quit
		if utblcorre.state.length()>2 set vRM = $$^MSG(1076,2) do vdderr("STATE", vRM) quit
		if utblcorre.telexadd.length()>20 set vRM = $$^MSG(1076,20) do vdderr("TELEXADD", vRM) quit
	}
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLCORRE","MSG",979,"UTBLCORRE."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLCORRE utblcorre

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux
	type String voldpar = vpar.get()                       // Save filer switches

	set vux = vx("KEY")
	set voldkey = vux.piece("|",1),vobj(utblcorre,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	do vload                                               // Make sure all data is loaded locally
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(utblcorre,-3) = vnewkey // Copy new keys into object
	type RecordUTBLCORRE vnewrec = utblcorre.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLCORRE",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update
	set vpar = voldpar
	if vpar["/TRIGAFT/" do VAU

	set vobj(utblcorre,-3) = vux.piece("|",1)              // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit
