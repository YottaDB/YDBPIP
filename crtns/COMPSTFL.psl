COMPSTFL(RecordDAYENDCOMPST dayendcompst, String vpar, Boolean vparNorm) // DAYENDCOMPST - Dayend Commission Posting File Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:14 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (14)             11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = dayendcompst.getMode()                // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(dayendcompst,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(dayendcompst,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("PSTDT").exists()!vx("AGENT").exists()!vx("CPLAN").exists()!vx("CID").exists()!vx("SEQ").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("DAYENDCOMPST",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("DAYENDCOMPST","PSTDT = :dayendcompst.pstdt and AGENT = :dayendcompst.agent and CPLAN = :dayendcompst.cplan and CID = :dayendcompst.cid and SEQ = :dayendcompst.seq") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String PSTDT
	type public String AGENT
	type public String CPLAN
	type public String CID
	type public String SEQ

	type RecordDAYENDCOMPST dayendcompst = Db.getRecord("DAYENDCOMPST","PSTDT=:PSTDT,AGENT=:AGENT,CPLAN=:CPLAN,CID=:CID,SEQ=:SEQ")
	if (%ProcessMode = 2) do {
		do dayendcompst.setMode(2)

		do COMPSTFL(dayendcompst,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordDAYENDCOMPST dayendcompst

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("DAYENDCOMPST","PSTDT = :dayendcompst.pstdt and AGENT = :dayendcompst.agent and CPLAN = :dayendcompst.cplan and CID = :dayendcompst.cid and SEQ = :dayendcompst.seq") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(dayendcompst)) S ^DAYEND(vobj(dayendcompst,-3),"COMPST",vobj(dayendcompst,-4),vobj(dayendcompst,-5),vobj(dayendcompst,-6),vobj(dayendcompst,-7))=vobj(dayendcompst)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordDAYENDCOMPST dayendcompst


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^DAYEND(vobj(dayendcompst,-3),"COMPST",vobj(dayendcompst,-4),vobj(dayendcompst,-5),vobj(dayendcompst,-6),vobj(dayendcompst,-7))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordDAYENDCOMPST dayendcompst

	// Type local variables for access keys for defaults
	type public String PSTDT
	type public String AGENT
	type public String CPLAN
	type public String CID
	type public String SEQ

	if dayendcompst.man.isNull() set dayendcompst.man = 0  // man
	if dayendcompst.posted.isNull() set dayendcompst.posted = 0 // posted
	quit


vreqn // Validate required data items

	type public RecordDAYENDCOMPST dayendcompst

	if dayendcompst.man.isNull() do vreqerr("MAN") quit
	if dayendcompst.posted.isNull() do vreqerr("POSTED") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordDAYENDCOMPST dayendcompst

	if dayendcompst.pstdt.isNull() do vreqerr("PSTDT") quit
	if dayendcompst.agent.isNull() do vreqerr("AGENT") quit
	if dayendcompst.cplan.isNull() do vreqerr("CPLAN") quit
	if dayendcompst.cid.isNull() do vreqerr("CID") quit
	if dayendcompst.seq.isNull() do vreqerr("SEQ") quit

		if vx("MAN").exists(),dayendcompst.man.isNull() do vreqerr("MAN") quit
		if vx("POSTED").exists(),dayendcompst.posted.isNull() do vreqerr("POSTED") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DAYENDCOMPST","MSG",1767,"DAYENDCOMPST."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordDAYENDCOMPST dayendcompst

	set X = dayendcompst.pstdt if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("PSTDT", vRM) quit
	set X = dayendcompst.agent if 'X.isNull(),X'?1.6N,X'?1"-"1.5N set vRM=$$^MSG(742,"N") do vdderr("AGENT", vRM) quit
	if dayendcompst.cplan.length()>8 set vRM = $$^MSG(1076,8) do vdderr("CPLAN", vRM) quit
	set X = dayendcompst.cid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("CID", vRM) quit
	set X = dayendcompst.seq if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("SEQ", vRM) quit
	set X = dayendcompst.agncyamt if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"DAYENDCOMPST.AGNCYAMT"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = dayendcompst.amt if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"DAYENDCOMPST.AMT"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	if dayendcompst.crcd.length()>3 set vRM = $$^MSG(1076,3) do vdderr("CRCD", vRM) quit
	if dayendcompst.index.length()>12 set vRM = $$^MSG(1076,12) do vdderr("INDEX", vRM) quit
	if '("01"[dayendcompst.MAN) set vRM=$$^MSG(742,"L") do vdderr("MAN", vRM) quit
	if dayendcompst.manuid.length()>20 set vRM = $$^MSG(1076,20) do vdderr("MANUID", vRM) quit
	set X = dayendcompst.orgagent if 'X.isNull(),X'?1.6N,X'?1"-"1.5N set vRM=$$^MSG(742,"N") do vdderr("ORGAGENT", vRM) quit
	if '("01"[dayendcompst.POSTED) set vRM=$$^MSG(742,"L") do vdderr("POSTED", vRM) quit
	set X = dayendcompst.rate if 'X.isNull() set vRM = $$VAL^DBSVER("N",8,0,,,,,5) if 'vRM.isNull() set vRM = $$^MSG(979,"DAYENDCOMPST.RATE"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DAYENDCOMPST","MSG",979,"DAYENDCOMPST."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordDAYENDCOMPST dayendcompst

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("PSTDT").exists() set vux("PSTDT") = vx("PSTDT")
	if vx("AGENT").exists() set vux("AGENT") = vx("AGENT")
	if vx("CPLAN").exists() set vux("CPLAN") = vx("CPLAN")
	if vx("CID").exists() set vux("CID") = vx("CID")
	if vx("SEQ").exists() set vux("SEQ") = vx("SEQ")
	do vkey(1) set voldkey = dayendcompst.pstdt_","_dayendcompst.agent_","_dayendcompst.cplan_","_dayendcompst.cid_","_dayendcompst.seq // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = dayendcompst.pstdt_","_dayendcompst.agent_","_dayendcompst.cplan_","_dayendcompst.cid_","_dayendcompst.seq // Copy new keys into object
	type RecordDAYENDCOMPST vnewrec = dayendcompst.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("DAYENDCOMPST",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordDAYENDCOMPST dayendcompst

	if vux("PSTDT").exists() set dayendcompst.pstdt = vux("PSTDT").piece("|",i)
	if vux("AGENT").exists() set dayendcompst.agent = vux("AGENT").piece("|",i)
	if vux("CPLAN").exists() set dayendcompst.cplan = vux("CPLAN").piece("|",i)
	if vux("CID").exists() set dayendcompst.cid = vux("CID").piece("|",i)
	if vux("SEQ").exists() set dayendcompst.seq = vux("SEQ").piece("|",i)
	quit
