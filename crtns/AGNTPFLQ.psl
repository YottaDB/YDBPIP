AGNTPFLQ(RecordAGENTPFTQ agentpftq, String vpar, Boolean vparNorm) // AGENTPFTQ - Agent Portfolio Query  Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:13 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (7)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = agentpftq.getMode()                   // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(agentpftq,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(agentpftq,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("AGENT").exists()!vx("PFT").exists()!vx("WPFT").exists()!vx("WPQRYNUM").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("AGENTPFTQ",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("AGENTPFTQ","AGENT = :agentpftq.agent and PFT = :agentpftq.pft and WPFT = :agentpftq.wpft and WPQRYNUM = :agentpftq.wpqrynum") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String AGENT
	type public String PFT
	type public String WPFT
	type public String WPQRYNUM

	type RecordAGENTPFTQ agentpftq = Db.getRecord("AGENTPFTQ","AGENT=:AGENT,PFT=:PFT,WPFT=:WPFT,WPQRYNUM=:WPQRYNUM")
	if (%ProcessMode = 2) do {
		do agentpftq.setMode(2)

		do AGNTPFLQ(agentpftq,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordAGENTPFTQ agentpftq

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("AGENTPFTQ","AGENT = :agentpftq.agent and PFT = :agentpftq.pft and WPFT = :agentpftq.wpft and WPQRYNUM = :agentpftq.wpqrynum") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(agentpftq)) S ^AGENT(vobj(agentpftq,-3),20,vobj(agentpftq,-4),vobj(agentpftq,-5),vobj(agentpftq,-6))=vobj(agentpftq)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordAGENTPFTQ agentpftq


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^AGENT(vobj(agentpftq,-3),20,vobj(agentpftq,-4),vobj(agentpftq,-5),vobj(agentpftq,-6))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordAGENTPFTQ agentpftq

	// Type local variables for access keys for defaults
	type public String AGENT
	type public String PFT
	type public String WPFT
	type public String WPQRYNUM

	if agentpftq.wpqrynum.isNull() set agentpftq.wpqrynum = 1 // wpqrynum
	quit


vreqn // Validate required data items

	type public RecordAGENTPFTQ agentpftq

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordAGENTPFTQ agentpftq

	if agentpftq.agent.isNull() do vreqerr("AGENT") quit
	if agentpftq.pft.isNull() do vreqerr("PFT") quit
	if agentpftq.wpft.isNull() do vreqerr("WPFT") quit
	if agentpftq.wpqrynum.isNull() do vreqerr("WPQRYNUM") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("AGENTPFTQ","MSG",1767,"AGENTPFTQ."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordAGENTPFTQ agentpftq

	set X = agentpftq.agent if 'X.isNull(),'Db.isDefined("AGENT","X") set vRM = $$^MSG(1485,X) do vdderr("AGENT", vRM) quit
	if agentpftq.pft.length()>12 set vRM = $$^MSG(1076,12) do vdderr("PFT", vRM) quit
	if agentpftq.wpft.length()>12 set vRM = $$^MSG(1076,12) do vdderr("WPFT", vRM) quit
	set X = agentpftq.wpqrynum if 'X.isNull(),X'?1.3N,X'?1"-"1.2N set vRM=$$^MSG(742,"N") do vdderr("WPQRYNUM", vRM) quit
	set X = agentpftq.qrybal if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"AGENTPFTQ.QRYBAL"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = agentpftq.wpagnfac if 'X.isNull() set vRM = $$VAL^DBSVER("N",8,0,,,,,5) if 'vRM.isNull() set vRM = $$^MSG(979,"AGENTPFTQ.WPAGNFAC"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	if agentpftq.wpqrynam.length()>12 set vRM = $$^MSG(1076,12) do vdderr("WPQRYNAM", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("AGENTPFTQ","MSG",979,"AGENTPFTQ."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordAGENTPFTQ agentpftq

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("AGENT").exists() set vux("AGENT") = vx("AGENT")
	if vx("PFT").exists() set vux("PFT") = vx("PFT")
	if vx("WPFT").exists() set vux("WPFT") = vx("WPFT")
	if vx("WPQRYNUM").exists() set vux("WPQRYNUM") = vx("WPQRYNUM")
	do vkey(1) set voldkey = agentpftq.agent_","_agentpftq.pft_","_agentpftq.wpft_","_agentpftq.wpqrynum // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = agentpftq.agent_","_agentpftq.pft_","_agentpftq.wpft_","_agentpftq.wpqrynum // Copy new keys into object
	type RecordAGENTPFTQ vnewrec = agentpftq.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("AGENTPFTQ",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordAGENTPFTQ agentpftq

	if vux("AGENT").exists() set agentpftq.agent = vux("AGENT").piece("|",i)
	if vux("PFT").exists() set agentpftq.pft = vux("PFT").piece("|",i)
	if vux("WPFT").exists() set agentpftq.wpft = vux("WPFT").piece("|",i)
	if vux("WPQRYNUM").exists() set agentpftq.wpqrynum = vux("WPQRYNUM").piece("|",i)
	quit
