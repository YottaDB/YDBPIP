EFUNDSSF(RecordEFUNDSSOAP efundssoap, String vpar, Boolean vparNorm) // EFUNDSSOAP - eFunds SOAP Message Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:16 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (2)              08/19/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = efundssoap.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(efundssoap,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(efundssoap,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("APPID").exists()!vx("SEQUENCE").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("EFUNDSSOAP",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("EFUNDSSOAP","APPID = :efundssoap.appid and SEQUENCE = :efundssoap.sequence") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String APPID
	type public String SEQUENCE

	type RecordEFUNDSSOAP efundssoap = Db.getRecord("EFUNDSSOAP","APPID=:APPID,SEQUENCE=:SEQUENCE")
	if (%ProcessMode = 2) do {
		do efundssoap.setMode(2)

		do EFUNDSSF(efundssoap,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordEFUNDSSOAP efundssoap

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("EFUNDSSOAP","APPID = :efundssoap.appid and SEQUENCE = :efundssoap.sequence") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(efundssoap)) K:$D(vobj(efundssoap,1,1)) ^EFSOAP(vobj(efundssoap,-3),vobj(efundssoap,-4)) S ^EFSOAP(vobj(efundssoap,-3),vobj(efundssoap,-4))=vobj(efundssoap)
		#ENDBYPASS
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
			if $D(vobj(efundssoap,1,1)) N vS1,vS2 S vS1=0 F vS2=1:450:$L(vobj(efundssoap,1,1)) S vS1=vS1+1,^EFSOAP(vobj(efundssoap,-3),vobj(efundssoap,-4),vS1)=$E(vobj(efundssoap,1,1),vS2,vS2+449)
			#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordEFUNDSSOAP efundssoap


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^EFSOAP(vobj(efundssoap,-3),vobj(efundssoap,-4))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordEFUNDSSOAP efundssoap

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordEFUNDSSOAP efundssoap

	if efundssoap.appid.isNull() do vreqerr("APPID") quit
	if efundssoap.sequence.isNull() do vreqerr("SEQUENCE") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EFUNDSSOAP","MSG",1767,"EFUNDSSOAP."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordEFUNDSSOAP efundssoap

	set X = efundssoap.appid if 'X.isNull(),'Db.isDefined("APPMGMT","X") set vRM = $$^MSG(1485,X) do vdderr("APPID", vRM) quit
	set X = efundssoap.sequence if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("SEQUENCE", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EFUNDSSOAP","MSG",979,"EFUNDSSOAP."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordEFUNDSSOAP efundssoap

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("APPID").exists() set vux("APPID") = vx("APPID")
	if vx("SEQUENCE").exists() set vux("SEQUENCE") = vx("SEQUENCE")
	do vkey(1) set voldkey = efundssoap.appid_","_efundssoap.sequence // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = efundssoap.appid_","_efundssoap.sequence // Copy new keys into object
	type RecordEFUNDSSOAP vnewrec = efundssoap.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("EFUNDSSOAP",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordEFUNDSSOAP efundssoap

	if vux("APPID").exists() set efundssoap.appid = vux("APPID").piece("|",i)
	if vux("SEQUENCE").exists() set efundssoap.sequence = vux("SEQUENCE").piece("|",i)
	quit
