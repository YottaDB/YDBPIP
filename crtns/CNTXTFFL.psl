CNTXTFFL(RecordSTBLCNTXTFLD stblcntxtfld, String vpar, Boolean vparNorm) // STBLCNTXTFLD - Context Fields Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:20 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (4)              10/05/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = stblcntxtfld.getMode()                // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(stblcntxtfld,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(stblcntxtfld,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CNTXTID").exists()!vx("TABLENAME").exists()!vx("COLUMNNAME").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("STBLCNTXTFLD",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("STBLCNTXTFLD","CNTXTID = :stblcntxtfld.cntxtid and TABLENAME = :stblcntxtfld.tablename and COLUMNNAME = :stblcntxtfld.columnname") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CNTXTID
	type public String TABLENAME
	type public String COLUMNNAME

	type RecordSTBLCNTXTFLD stblcntxtfld = Db.getRecord("STBLCNTXTFLD","CNTXTID=:CNTXTID,TABLENAME=:TABLENAME,COLUMNNAME=:COLUMNNAME")
	if (%ProcessMode = 2) do {
		do stblcntxtfld.setMode(2)

		do CNTXTFFL(stblcntxtfld,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordSTBLCNTXTFLD stblcntxtfld

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("STBLCNTXTFLD","CNTXTID = :stblcntxtfld.cntxtid and TABLENAME = :stblcntxtfld.tablename and COLUMNNAME = :stblcntxtfld.columnname") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtfld,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtfld,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(stblcntxtfld)) S ^STBL("CNTXTFLD",vobj(stblcntxtfld,-3),vobj(stblcntxtfld,-4),vobj(stblcntxtfld,-5))=vobj(stblcntxtfld)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordSTBLCNTXTFLD stblcntxtfld

	if vpar["/CASDEL/" do VCASDEL                          // Cascade delete
	if vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtfld,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^STBL("CNTXTFLD",vobj(stblcntxtfld,-3),vobj(stblcntxtfld,-4),vobj(stblcntxtfld,-5))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordSTBLCNTXTFLD stblcntxtfld

	if stblcntxtfld.minimumperm.isNull() do vreqerr("MINIMUMPERM") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordSTBLCNTXTFLD stblcntxtfld

	if stblcntxtfld.cntxtid.isNull() do vreqerr("CNTXTID") quit
	if stblcntxtfld.tablename.isNull() do vreqerr("TABLENAME") quit
	if stblcntxtfld.columnname.isNull() do vreqerr("COLUMNNAME") quit

		if vx("MINIMUMPERM").exists(),stblcntxtfld.minimumperm.isNull() do vreqerr("MINIMUMPERM") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("STBLCNTXTFLD","MSG",1767,"STBLCNTXTFLD."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordSTBLCNTXTFLD stblcntxtfld
	if 'stblcntxtfld.cntxtid.isNull() set vfkey("^STBL("_"""CNTXT"""_","_""""_stblcntxtfld.cntxtid_""""_")")="STBLCNTXTFLD(CNTXTID) -> STBLCNTXT"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordSTBLCNTXTFLD stblcntxtfld
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("STBLCNTXT",":stblcntxtfld.cntxtid") set vERRMSG = $$^MSG(8563,"STBLCNTXTFLD(CNTXTID) -> STBLCNTXT") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordSTBLCNTXTFLD stblcntxtfld

	set X = stblcntxtfld.cntxtid if 'X.isNull(),'Db.isDefined("STBLCNTXT","X") set vRM = $$^MSG(1485,X) do vdderr("CNTXTID", vRM) quit
	if stblcntxtfld.tablename.length()>12 set vRM = $$^MSG(1076,12) do vdderr("TABLENAME", vRM) quit
	if stblcntxtfld.columnname.length()>12 set vRM = $$^MSG(1076,12) do vdderr("COLUMNNAME", vRM) quit
	set X = stblcntxtfld.minimumperm if 'X.isNull(),'Db.isDefined("STBLCNTXTPER","X") set vRM = $$^MSG(1485,X) do vdderr("MINIMUMPERM", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("STBLCNTXTFLD","MSG",979,"STBLCNTXTFLD."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordSTBLCNTXTFLD stblcntxtfld

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CNTXTID").exists() set vux("CNTXTID") = vx("CNTXTID")
	if vx("TABLENAME").exists() set vux("TABLENAME") = vx("TABLENAME")
	if vx("COLUMNNAME").exists() set vux("COLUMNNAME") = vx("COLUMNNAME")
	do vkey(1) set voldkey = stblcntxtfld.cntxtid_","_stblcntxtfld.tablename_","_stblcntxtfld.columnname // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = stblcntxtfld.cntxtid_","_stblcntxtfld.tablename_","_stblcntxtfld.columnname // Copy new keys into object
	type RecordSTBLCNTXTFLD vnewrec = stblcntxtfld.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("STBLCNTXTFLD",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordSTBLCNTXTFLD stblcntxtfld

	if vux("CNTXTID").exists() set stblcntxtfld.cntxtid = vux("CNTXTID").piece("|",i)
	if vux("TABLENAME").exists() set stblcntxtfld.tablename = vux("TABLENAME").piece("|",i)
	if vux("COLUMNNAME").exists() set stblcntxtfld.columnname = vux("COLUMNNAME").piece("|",i)
	quit

VCASDEL // Cascade delete logic

	type public RecordSTBLCNTXTFLD stblcntxtfld
	type public String vpar

	do Db.delete("UTBLCNTXTFDR","CNTXTID=:stblcntxtfld.cntxtid AND TABLENAME=:stblcntxtfld.tablename AND COLUMNNAME=:stblcntxtfld.columnname",vpar) // Cascade delete

	quit
