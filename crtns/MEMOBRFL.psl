MEMOBRFL(RecordMEMOBROKER memobroker, String vpar, Boolean vparNorm) // MEMOBROKER - Memo Table for Broker Accounts Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:19 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (8)              11/22/2003
	// Trigger Definition (2)                      01/12/2005
	// Journal File Definition (3)                 01/12/2005
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = memobroker.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(memobroker,.vxins(),10,"|")
	if %O = 1 quit:'memobroker.isChanged()  do AUDIT^UCUTILN(memobroker,.vx(),10,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	// Define local variables for access keys for legacy triggers
	type String SERVICE = memobroker.service
	type String ACCKEY = memobroker.acckey

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("SERVICE").exists()!vx("ACCKEY").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("MEMOBROKER",.vx)
		set %O = 1 do vexec
		if vpar["/TRIGAFT/" do VAU                            // After update triggers
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("MEMOBROKER","SERVICE = :memobroker.service and ACCKEY = :memobroker.acckey") // No record exists
		do vdelete(0)
		if vpar["/TRIGAFT/" do VAD                            // After delete triggers
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String SERVICE
	type public String ACCKEY

	type RecordMEMOBROKER memobroker = Db.getRecord("MEMOBROKER","SERVICE=:SERVICE,ACCKEY=:ACCKEY")
	if (%ProcessMode = 2) do {
		do memobroker.setMode(2)

		do MEMOBRFL(memobroker,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordMEMOBROKER memobroker

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("MEMOBROKER","SERVICE = :memobroker.service and ACCKEY = :memobroker.acckey") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		type String n = -1
		type String x

		if %O = 0 for  set n = vobj(memobroker,n).order() quit:n.isNull()  do {
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^MEMO("BROKER",vobj(memobroker,-3),vobj(memobroker,-4),n)=vobj(memobroker,n)
			#ENDBYPASS
		}

		else  for  set n = vobj(memobroker,-100,n).order() quit:n.isNull()  do {
			quit:'$D(vobj(memobroker,n))
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^MEMO("BROKER",vobj(memobroker,-3),vobj(memobroker,-4),n)=vobj(memobroker,n)
			#ENDBYPASS
		}


		if vpar["/JOURNAL/" do VJOURNAL(.memobroker)          // Create journal files
	}

	quit

vload // Record Load - force loading of unloaded data

	type public RecordMEMOBROKER memobroker
	type String n = ""

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	for  set n=$order(^MEMO("BROKER",vobj(memobroker,-3),vobj(memobroker,-4),n)) quit:n=""  if '$D(vobj(memobroker,n)),$D(^MEMO("BROKER",vobj(memobroker,-3),vobj(memobroker,-4),n))#2 set vobj(memobroker,n)=^(n)
	#ENDBYPASS
	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordMEMOBROKER memobroker

	if 'vkeychg.get(),memobroker.isChanged() throw Class.new("Error","%PSL-E-DBFILER,Deleted object cannot be modified")

	if vpar["/JOURNAL/" do VJOURNAL(.memobroker)           // Create journal entries

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^MEMO("BROKER",vobj(memobroker,-3),vobj(memobroker,-4))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordMEMOBROKER memobroker

	// Type local variables for access keys for defaults
	type public String SERVICE
	type public String ACCKEY

	type public String TJD
	if memobroker.odt.isNull() set memobroker.odt = TJD    // odt
	quit


vreqn // Validate required data items

	type public RecordMEMOBROKER memobroker

	if memobroker.acn.isNull() do vreqerr("ACN") quit
	if memobroker.identifier.isNull() do vreqerr("IDENTIFIER") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordMEMOBROKER memobroker

	if memobroker.service.isNull() do vreqerr("SERVICE") quit
	if memobroker.acckey.isNull() do vreqerr("ACCKEY") quit

	if 'vobj(memobroker,-100,1,"").order().isNull() do {
		if vx("ACN").exists(),memobroker.acn.isNull() do vreqerr("ACN") quit
		if vx("IDENTIFIER").exists(),memobroker.identifier.isNull() do vreqerr("IDENTIFIER") quit
	}
		if vx("ACN").exists(),memobroker.acn.isNull() do vreqerr("ACN") quit
		if vx("IDENTIFIER").exists(),memobroker.identifier.isNull() do vreqerr("IDENTIFIER") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("MEMOBROKER","MSG",1767,"MEMOBROKER."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VAD //
	type public Number ER = 0
	type public String vx(),RM
	do vad1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit

VAU //
	type public Number ER = 0
	type public String vx(),RM
	if vx("").order().isNull() quit
	if vx("ACCKEY").exists()!vx("SERVICE").exists()!vx("IDENTIFIER").exists() do vau1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vad1 // Trigger AFTER_DELETE - After delete trigger

	type public RecordMEMOBROKER memobroker
	// Delete CIFACCTS entries for this memo table
	new XACN,XSRVCAT,XSERVICE,XACCKEY

	set XSRVCAT="BROKER"
	set XSERVICE=memobroker.service
	set XACCKEY=memobroker.acckey
	
	new rs 
	type ResultSet rs=Db.select("ACN","CIFACCTS","SRVCAT=:XSRVCAT AND SERVICE=:XSERVICE AND ACCKEY=:XACCKEY")
	if rs.isEmpty() quit
	while rs.next() do { 
		do Db.delete("CIFACCTS","SRVCAT=:XSRVCAT AND SERVICE=:XSERVICE AND ACCKEY=:XACCKEY AND ACN=:XACN")
		}
	quit

vau1 // Trigger AFTER_UPDATE - After Update Trigger

	type public RecordMEMOBROKER memobroker
	/*
	------ Revision History -----------------------------------------------------
	
	01/13/00 - SHANL - 31126
		   Fixed undefined record by adding method Db.isDefined.            	

	09/17/99 - CARROLLJ - 31126
		   Convert to PSL.

	08/12/99 - CONARTYR - 34434
		   Update CIFACCTS entries for this memo table
	*/

	new OLDAKEY,OLDSRV,X,XACN,XSRVCAT,XSERVICE,XACCKEY,XIDEN
	set XSRVCAT="BROKER"
	set XSERVICE=memobroker.service
	set XACCKEY=memobroker.acckey
	set XACN=memobroker.acn
	set XIDEN=memobroker.identifier
	set OLDAKEY=memobroker.acckey.oldVal
	set OLDSRV=memobroker.service.oldVal

	if Db.isDefined("CIFACCTS","XACN,XSRVCAT,XSERVICE,XACCKEY") do {
		do Db.update("CIFACCTS","SERVICE=:XSERVICE,ACCKEY=:XACCKEY,IDENTIFIER=:XIDEN","ACN=:XACN AND SRVCAT=:XSRVCAT AND ACCKEY=:OLDAKEY AND SERVICE=:OLDSRV")
		}
	quit
	


vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordMEMOBROKER memobroker

	if (%O = 2) do vload

	if vobj(memobroker,1).exists() do {

		if memobroker.acctname.length()>25 set vRM = $$^MSG(1076,25) do vdderr("ACCTNAME", vRM) quit
		set X = memobroker.acn if 'X.isNull(),'Db.isDefined("CIF","X") set vRM = $$^MSG(1485,X) do vdderr("ACN", vRM) quit
		if memobroker.identifier.length()>25 set vRM = $$^MSG(1076,25) do vdderr("IDENTIFIER", vRM) quit
		set X = memobroker.memorelc if 'X.isNull(),'Db.isDefined("UTBLRELSRV","X") set vRM = $$^MSG(1485,X) do vdderr("MEMORELC", vRM) quit
		set X = memobroker.odt if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("ODT", vRM) quit
		set X = memobroker.status if 'X.isNull(),'Db.isDefined("STBLESTATUS","X") set vRM = $$^MSG(1485,X) do vdderr("STATUS", vRM) quit
	}
	if memobroker.service.length()>20 set vRM = $$^MSG(1076,20) do vdderr("SERVICE", vRM) quit
	if memobroker.acckey.length()>40 set vRM = $$^MSG(1076,40) do vdderr("ACCKEY", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("MEMOBROKER","MSG",979,"MEMOBROKER."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit

VJOURNAL(RecordMEMOBROKER memobroker)	//MEMOBROKER Journal file entries

	type Public Date %EffectiveDate
	type Public String %TSRC,vpar,vx()
	type String TSRC,vdi,vdx()

	if %TSRC.get().isNull() set TSRC="O"
	else  set TSRC=%TSRC

	if %ProcessMode=3 do {
		if TSRC="B" do {
			do vj1(.memobroker)	// Mode=D Tran=B EFD=N,E Seq=1 JRNID=CIFH_D
			}
		else  if TSRC="O" do {
			do vj1(.memobroker)	// Mode=D Tran=O EFD=N,E Seq=1 JRNID=CIFH_D
			}
		}
	else  if %ProcessMode=0 do {
		if TSRC="B" do {
			do vj2(.memobroker)	// Mode=I Tran=B EFD=N,E Seq=1 JRNID=CIFH_I
			}
		else  if TSRC="O" do {
			do vj2(.memobroker)	// Mode=I Tran=O EFD=N,E Seq=1 JRNID=CIFH_I
			}
		}
	else  if %ProcessMode=1 do {
		if TSRC="B" do {
			do vj3(.memobroker)	// Mode=U Tran=B EFD=N,E Seq=1 JRNID=CIFH_U
			}
		else  if TSRC="O" do {
			do vj3(.memobroker)	// Mode=U Tran=O EFD=N,E Seq=1 JRNID=CIFH_U
			}
		}

	quit


vj1(RecordMEMOBROKER memobroker)	// CIFH_D  Table CIFH  Delete History

	type Public String EFD,%IDENT,%UID,TJD,TLO
	type String v1,vlastkey
	set v1=memobroker.acn
	set vlastkey=Db.nextVal("CIFH","ACN=:v1")
	type RecordCIFH cifh=Db.getRecord("CIFH","ACN=:v1,SEQ=:vlastkey",1)
	set cifh.efd=$G(EFD)
	set cifh.hdate=+$H
	set cifh.htime=$P($H,",",2)
	set cifh.ident=%IDENT
	set cifh.tcmt=$$^MSG(3872,"MEMOBROKER",memobroker.service,memobroker.acckey)
	set cifh.tjd=TJD
	set cifh.tlo=TLO
	set cifh.tso=$$^MSG(3873,"MEMOBROKER",memobroker.service,memobroker.acckey)
	set cifh.uid=%UID

	do cifh.save("/NOVALFK/NOVALDD/NOVALRI")

	quit


vj2(RecordMEMOBROKER memobroker)	// CIFH_I  Table CIFH  Insert History

	type Public String EFD,%IDENT,%UID,TJD,TLO
	type String v1,vlastkey
	set v1=memobroker.acn
	set vlastkey=Db.nextVal("CIFH","ACN=:v1")
	type RecordCIFH cifh=Db.getRecord("CIFH","ACN=:v1,SEQ=:vlastkey",1)
	set cifh.efd=$G(EFD)
	set cifh.hdate=+$H
	set cifh.htime=$P($H,",",2)
	set cifh.ident=%IDENT
	set cifh.tcmt=$$^MSG(3870,"MEMOBROKER",memobroker.service,memobroker.acckey)
	set cifh.tjd=TJD
	set cifh.tlo=TLO
	set cifh.tso=$$^MSG(3873,"MEMOBROKER",memobroker.service,memobroker.acckey)
	set cifh.uid=%UID

	do cifh.save("/NOVALFK/NOVALDD/NOVALRI")

	quit


vj3(RecordMEMOBROKER memobroker)	// CIFH_U  Table CIFH  Maintenance History

	type Public String vx()
	type String vdi

	set vdi="" for  set vdi=vx(vdi).order() quit:vdi=""  do {
		type Public String vx(),EFD,%IDENT,%UID,TJD,TLO
		type String v1,vlastkey

		type String vold,vnew

		set vold=vx(vdi).piece("|",1)
		set vnew=vx(vdi).piece("|",2)

		set v1=memobroker.acn
		set vlastkey=Db.nextVal("CIFH","ACN=:v1")
		type RecordCIFH cifh=Db.getRecord("CIFH","ACN=:v1,SEQ=:vlastkey",1)
		set cifh.efd=$G(EFD)
		set cifh.hdate=+$H
		set cifh.htime=$P($H,",",2)
		set cifh.ident=%IDENT
		set cifh.tcmt=$$TCMT^CIFFUNCS(memobroker.service_","_memobroker.acckey,"MEMOBROKER",vdi,vold,vnew,$G(EFD))
		set cifh.tjd=TJD
		set cifh.tlo=TLO
		set cifh.tso=$$^MSG(3873,"MEMOBROKER",memobroker.service,memobroker.acckey)
		set cifh.uid=%UID

		do cifh.save("/NOVALFK/NOVALDD/NOVALRI")
		}

	quit



vkchged // Access key changed

	type public RecordMEMOBROKER memobroker

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux
	type String voldpar = vpar.get()                       // Save filer switches

	if vx("SERVICE").exists() set vux("SERVICE") = vx("SERVICE")
	if vx("ACCKEY").exists() set vux("ACCKEY") = vx("ACCKEY")
	do vkey(1) set voldkey = memobroker.service_","_memobroker.acckey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	do vload                                               // Make sure all data is loaded locally
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = memobroker.service_","_memobroker.acckey // Copy new keys into object
	type RecordMEMOBROKER vnewrec = memobroker.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("MEMOBROKER",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update
	set vpar = voldpar
	if vpar["/TRIGAFT/" do VAU

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordMEMOBROKER memobroker

	if vux("SERVICE").exists() set memobroker.service = vux("SERVICE").piece("|",i)
	if vux("ACCKEY").exists() set memobroker.acckey = vux("ACCKEY").piece("|",i)
	quit
