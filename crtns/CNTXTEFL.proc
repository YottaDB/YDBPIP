CNTXTEFL(RecordSTBLCNTXTEVT stblcntxtevt, String vpar, Boolean vparNorm) // STBLCNTXTEVT - Context Events Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 01/31/2007 11:24 - shetyes
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (3)              09/13/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = stblcntxtevt.getMode()                // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar
	if %O = 0 do AUDIT^UCUTILN(stblcntxtevt,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(stblcntxtevt,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CNTXTID").exists()!vx("EVTID").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("STBLCNTXTEVT",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("STBLCNTXTEVT","CNTXTID = :stblcntxtevt.cntxtid and EVTID = :stblcntxtevt.evtid") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CNTXTID
	type public String EVTID

	type RecordSTBLCNTXTEVT stblcntxtevt = Db.getRecord("STBLCNTXTEVT","CNTXTID=:CNTXTID,EVTID=:EVTID")
	if (%ProcessMode = 2) do {
		do stblcntxtevt.setMode(2)

		do CNTXTEFL(stblcntxtevt,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordSTBLCNTXTEVT stblcntxtevt

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("STBLCNTXTEVT","CNTXTID = :stblcntxtevt.cntxtid and EVTID = :stblcntxtevt.evtid") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBSFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtevt,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtevt,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(stblcntxtevt))  S ^STBL("CNTXTEVT",vobj(stblcntxtevt,-3),vobj(stblcntxtevt,-4))=vobj(stblcntxtevt)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordSTBLCNTXTEVT stblcntxtevt

	if vpar'["/NOLOG/" do ^DBSLOGIT(stblcntxtevt,3)

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602
	ZWI ^STBL("CNTXTEVT",vobj(stblcntxtevt,-3),vobj(stblcntxtevt,-4)) // Delete Record
	quit


vreqn // Validate required data items

	type public RecordSTBLCNTXTEVT stblcntxtevt

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordSTBLCNTXTEVT stblcntxtevt

	if stblcntxtevt.cntxtid.isNull() do vreqerr("CNTXTID") quit
	if stblcntxtevt.evtid.isNull() do vreqerr("EVTID") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("STBLCNTXTEVT","MSG",1767,"STBLCNTXTEVT."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordSTBLCNTXTEVT stblcntxtevt
	if 'stblcntxtevt.cntxtid.isNull() set vfkey("^STBL("_"""CNTXT"""_","_""""_stblcntxtevt.cntxtid_""""_")")="STBLCNTXTEVT(CNTXTID) -> STBLCNTXT"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordSTBLCNTXTEVT stblcntxtevt
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("STBLCNTXT",":stblcntxtevt.cntxtid") set vERRMSG = $$^MSG(8563,"STBLCNTXTEVT(CNTXTID) -> STBLCNTXT") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordSTBLCNTXTEVT stblcntxtevt

	set X = stblcntxtevt.cntxtid if 'X.isNull(),'Db.isDefined("STBLCNTXT","X") set vRM = $$^MSG(1485,X) do vdderr("CNTXTID", vRM) quit
	if stblcntxtevt.evtid.length()>12 set vRM = $$^MSG(1076,12) do vdderr("EVTID", vRM) quit
	set X = stblcntxtevt.minimumperm if 'X.isNull(),'Db.isDefined("STBLCNTXTPER","X") set vRM = $$^MSG(1485,X) do vdderr("MINIMUMPERM", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("STBLCNTXTEVT","MSG",979,"STBLCNTXTEVT."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordSTBLCNTXTEVT stblcntxtevt

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CNTXTID").exists() set vux("CNTXTID") = vx("CNTXTID")
	if vx("EVTID").exists() set vux("EVTID") = vx("EVTID")
	do vkey(1) set voldkey = stblcntxtevt.cntxtid_","_stblcntxtevt.evtid // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = stblcntxtevt.cntxtid_","_stblcntxtevt.evtid // Copy new keys into object
	type RecordSTBLCNTXTEVT vnewrec = stblcntxtevt.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("STBLCNTXTEVT",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordSTBLCNTXTEVT stblcntxtevt

	if vux("CNTXTID").exists() set stblcntxtevt.cntxtid = vux("CNTXTID").piece("|",i)
	if vux("EVTID").exists() set stblcntxtevt.evtid = vux("EVTID").piece("|",i)
	quit
