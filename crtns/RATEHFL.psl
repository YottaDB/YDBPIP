RATEHFL(RecordCRCDRATEH crcdrateh, String vpar, Boolean vparNorm) // CRCDRATEH - Currency Code Rates Table Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:13 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (16)             01/19/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = crcdrateh.getMode()                   // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(crcdrateh,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(crcdrateh,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CO").exists()!vx("CRCD").exists()!vx("EFD").exists()!vx("SEQ").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("CRCDRATEH",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("CRCDRATEH","CO = :crcdrateh.co and CRCD = :crcdrateh.crcd and EFD = :crcdrateh.efd and SEQ = :crcdrateh.seq") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CO
	type public String CRCD
	type public String EFD
	type public String SEQ

	type RecordCRCDRATEH crcdrateh = Db.getRecord("CRCDRATEH","CO=:CO,CRCD=:CRCD,EFD=:EFD,SEQ=:SEQ")
	if (%ProcessMode = 2) do {
		do crcdrateh.setMode(2)

		do RATEHFL(crcdrateh,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordCRCDRATEH crcdrateh

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("CRCDRATEH","CO = :crcdrateh.co and CRCD = :crcdrateh.crcd and EFD = :crcdrateh.efd and SEQ = :crcdrateh.seq") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(crcdrateh,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(crcdrateh,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(crcdrateh)) S ^CRCDH(vobj(crcdrateh,-3),vobj(crcdrateh,-4),1,vobj(crcdrateh,-5),vobj(crcdrateh,-6))=vobj(crcdrateh)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordCRCDRATEH crcdrateh

	if vpar'["/NOLOG/" do ^DBSLOGIT(crcdrateh,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^CRCDH(vobj(crcdrateh,-3),vobj(crcdrateh,-4),1,vobj(crcdrateh,-5),vobj(crcdrateh,-6))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordCRCDRATEH crcdrateh

	// Type local variables for access keys for defaults
	type public String CO
	type public String CRCD
	type public String EFD
	type public String SEQ

	if crcdrateh.co.isNull() set crcdrateh.co = CUVAR.CO   // co
	if crcdrateh.terms.isNull() set crcdrateh.terms = 0    // terms
	quit


vreqn // Validate required data items

	type public RecordCRCDRATEH crcdrateh

	if crcdrateh.terms.isNull() do vreqerr("TERMS") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordCRCDRATEH crcdrateh

	if crcdrateh.co.isNull() do vreqerr("CO") quit
	if crcdrateh.crcd.isNull() do vreqerr("CRCD") quit
	if crcdrateh.efd.isNull() do vreqerr("EFD") quit
	if crcdrateh.seq.isNull() do vreqerr("SEQ") quit

		if vx("TERMS").exists(),crcdrateh.terms.isNull() do vreqerr("TERMS") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CRCDRATEH","MSG",1767,"CRCDRATEH."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordCRCDRATEH crcdrateh
	if 'crcdrateh.crcd.isNull() set vfkey("^CRCD("_""""_crcdrateh.co_""""_","_""""_crcdrateh.crcd_""""_")")="CRCDRATEH(CO,CRCD) -> CRCD"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordCRCDRATEH crcdrateh
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("CRCD",":crcdrateh.co,:crcdrateh.crcd") set vERRMSG = $$^MSG(8563,"CRCDRATEH(CO,CRCD) -> CRCD") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordCRCDRATEH crcdrateh

	if crcdrateh.co.length()>12 set vRM = $$^MSG(1076,12) do vdderr("CO", vRM) quit
	if crcdrateh.crcd.length()>3 set vRM = $$^MSG(1076,3) do vdderr("CRCD", vRM) quit
	set X = crcdrateh.efd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("EFD", vRM) quit
	set X = crcdrateh.seq if 'X.isNull(),X'?1.2N,X'?1"-"1.1N set vRM=$$^MSG(742,"N") do vdderr("SEQ", vRM) quit
	set X = crcdrateh.avespotaccum if 'X.isNull() set vRM = $$VAL^DBSVER("N",10,0,,,,,5) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.AVESPOTACCUM"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.dlm if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("DLM", vRM) quit
	set X = crcdrateh.finspot if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.FINSPOT"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.midrate if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.MIDRATE"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.offbid if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.OFFBID"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.offoffer if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.OFFOFFER"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.otcbid if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.OTCBID"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.otcoffer if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.OTCOFFER"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.spotbid if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.SPOTBID"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = crcdrateh.spotoffer if 'X.isNull() set vRM = $$VAL^DBSVER("N",13,0,,,.0000001,,7) if 'vRM.isNull() set vRM = $$^MSG(979,"CRCDRATEH.SPOTOFFER"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	if '("01"[crcdrateh.TERMS) set vRM=$$^MSG(742,"L") do vdderr("TERMS", vRM) quit
	set X = crcdrateh.tlm if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"C") do vdderr("TLM", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CRCDRATEH","MSG",979,"CRCDRATEH."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordCRCDRATEH crcdrateh

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CO").exists() set vux("CO") = vx("CO")
	if vx("CRCD").exists() set vux("CRCD") = vx("CRCD")
	if vx("EFD").exists() set vux("EFD") = vx("EFD")
	if vx("SEQ").exists() set vux("SEQ") = vx("SEQ")
	do vkey(1) set voldkey = crcdrateh.co_","_crcdrateh.crcd_","_crcdrateh.efd_","_crcdrateh.seq // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = crcdrateh.co_","_crcdrateh.crcd_","_crcdrateh.efd_","_crcdrateh.seq // Copy new keys into object
	type RecordCRCDRATEH vnewrec = crcdrateh.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("CRCDRATEH",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordCRCDRATEH crcdrateh

	if vux("CO").exists() set crcdrateh.co = vux("CO").piece("|",i)
	if vux("CRCD").exists() set crcdrateh.crcd = vux("CRCD").piece("|",i)
	if vux("EFD").exists() set crcdrateh.efd = vux("EFD").piece("|",i)
	if vux("SEQ").exists() set crcdrateh.seq = vux("SEQ").piece("|",i)
	quit
