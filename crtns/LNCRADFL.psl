LNCRADFL(RecordLNCRADTL lncradtl, String vpar, Boolean vparNorm) // LNCRADTL - CRA Loan Detail Table Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:18 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (13)             11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = lncradtl.getMode()                    // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(lncradtl,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(lncradtl,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CYEAR").exists()!vx("LFPC").exists()!vx("MSACD").exists()!vx("CRASTCD").exists()!vx("CRACNTCD").exists()!vx("CEN").exists()!vx("CID").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("LNCRADTL",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("LNCRADTL","CYEAR = :lncradtl.cyear and LFPC = :lncradtl.lfpc and MSACD = :lncradtl.msacd and CRASTCD = :lncradtl.crastcd and CRACNTCD = :lncradtl.cracntcd and CEN = :lncradtl.cen and CID = :lncradtl.cid") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CYEAR
	type public String LFPC
	type public String MSACD
	type public String CRASTCD
	type public String CRACNTCD
	type public String CEN
	type public String CID

	type RecordLNCRADTL lncradtl = Db.getRecord("LNCRADTL","CYEAR=:CYEAR,LFPC=:LFPC,MSACD=:MSACD,CRASTCD=:CRASTCD,CRACNTCD=:CRACNTCD,CEN=:CEN,CID=:CID")
	if (%ProcessMode = 2) do {
		do lncradtl.setMode(2)

		do LNCRADFL(lncradtl,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordLNCRADTL lncradtl

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("LNCRADTL","CYEAR = :lncradtl.cyear and LFPC = :lncradtl.lfpc and MSACD = :lncradtl.msacd and CRASTCD = :lncradtl.crastcd and CRACNTCD = :lncradtl.cracntcd and CEN = :lncradtl.cen and CID = :lncradtl.cid") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(lncradtl)) S ^LNCRA(vobj(lncradtl,-3),vobj(lncradtl,-4),vobj(lncradtl,-5),vobj(lncradtl,-6),vobj(lncradtl,-7),vobj(lncradtl,-8),vobj(lncradtl,-9))=vobj(lncradtl)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordLNCRADTL lncradtl


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^LNCRA(vobj(lncradtl,-3),vobj(lncradtl,-4),vobj(lncradtl,-5),vobj(lncradtl,-6),vobj(lncradtl,-7),vobj(lncradtl,-8),vobj(lncradtl,-9))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordLNCRADTL lncradtl

	// Type local variables for access keys for defaults
	type public String CYEAR
	type public String LFPC
	type public String MSACD
	type public String CRASTCD
	type public String CRACNTCD
	type public String CEN
	type public String CID

	if lncradtl.affln.isNull() set lncradtl.affln = 0      // affln
	if lncradtl.ppflg.isNull() set lncradtl.ppflg = 0      // ppflg
	quit


vreqn // Validate required data items

	type public RecordLNCRADTL lncradtl

	if lncradtl.affln.isNull() do vreqerr("AFFLN") quit
	if lncradtl.ppflg.isNull() do vreqerr("PPFLG") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordLNCRADTL lncradtl

	if lncradtl.cyear.isNull() do vreqerr("CYEAR") quit
	if lncradtl.lfpc.isNull() do vreqerr("LFPC") quit
	if lncradtl.msacd.isNull() do vreqerr("MSACD") quit
	if lncradtl.crastcd.isNull() do vreqerr("CRASTCD") quit
	if lncradtl.cracntcd.isNull() do vreqerr("CRACNTCD") quit
	if lncradtl.cen.isNull() do vreqerr("CEN") quit
	if lncradtl.cid.isNull() do vreqerr("CID") quit

		if vx("AFFLN").exists(),lncradtl.affln.isNull() do vreqerr("AFFLN") quit
		if vx("PPFLG").exists(),lncradtl.ppflg.isNull() do vreqerr("PPFLG") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("LNCRADTL","MSG",1767,"LNCRADTL."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordLNCRADTL lncradtl
	if 'lncradtl.cid.isNull() set vfkey("^ACN("_""""_lncradtl.cid_""""_","_50_")")="LNCRADTL(CID) -> ACN"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordLNCRADTL lncradtl
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("ACN",":lncradtl.cid") set vERRMSG = $$^MSG(8563,"LNCRADTL(CID) -> ACN") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordLNCRADTL lncradtl

	set X = lncradtl.cyear if 'X.isNull(),X'?1.4N,X'?1"-"1.3N set vRM=$$^MSG(742,"N") do vdderr("CYEAR", vRM) quit
	set X = lncradtl.lfpc if 'X.isNull(),'Db.isDefined("STBLLFSC1","X") set vRM = $$^MSG(1485,X) do vdderr("LFPC", vRM) quit
	set X = lncradtl.msacd if 'X.isNull(),'Db.isDefined("UTBLMSA","X") set vRM = $$^MSG(1485,X) do vdderr("MSACD", vRM) quit
	set X = lncradtl.crastcd if 'X.isNull(),X'?1.2N,X'?1"-"1.1N set vRM=$$^MSG(742,"N") do vdderr("CRASTCD", vRM) quit
	set X = lncradtl.cracntcd if 'X.isNull(),X'?1.3N,X'?1"-"1.2N set vRM=$$^MSG(742,"N") do vdderr("CRACNTCD", vRM) quit
	if lncradtl.cen.length()>7 set vRM = $$^MSG(1076,7) do vdderr("CEN", vRM) quit
	set X = lncradtl.cid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("CID", vRM) quit
	if '("01"[lncradtl.AFFLN) set vRM=$$^MSG(742,"L") do vdderr("AFFLN", vRM) quit
	set X = lncradtl.amtreq if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"LNCRADTL.AMTREQ"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = lncradtl.ansale if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"LNCRADTL.ANSALE"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = lncradtl.dtnt if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("DTNT", vRM) quit
	set X = lncradtl.odt if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("ODT", vRM) quit
	if '("01"[lncradtl.PPFLG) set vRM=$$^MSG(742,"L") do vdderr("PPFLG", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("LNCRADTL","MSG",979,"LNCRADTL."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordLNCRADTL lncradtl

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CYEAR").exists() set vux("CYEAR") = vx("CYEAR")
	if vx("LFPC").exists() set vux("LFPC") = vx("LFPC")
	if vx("MSACD").exists() set vux("MSACD") = vx("MSACD")
	if vx("CRASTCD").exists() set vux("CRASTCD") = vx("CRASTCD")
	if vx("CRACNTCD").exists() set vux("CRACNTCD") = vx("CRACNTCD")
	if vx("CEN").exists() set vux("CEN") = vx("CEN")
	if vx("CID").exists() set vux("CID") = vx("CID")
	do vkey(1) set voldkey = lncradtl.cyear_","_lncradtl.lfpc_","_lncradtl.msacd_","_lncradtl.crastcd_","_lncradtl.cracntcd_","_lncradtl.cen_","_lncradtl.cid // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = lncradtl.cyear_","_lncradtl.lfpc_","_lncradtl.msacd_","_lncradtl.crastcd_","_lncradtl.cracntcd_","_lncradtl.cen_","_lncradtl.cid // Copy new keys into object
	type RecordLNCRADTL vnewrec = lncradtl.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("LNCRADTL",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordLNCRADTL lncradtl

	if vux("CYEAR").exists() set lncradtl.cyear = vux("CYEAR").piece("|",i)
	if vux("LFPC").exists() set lncradtl.lfpc = vux("LFPC").piece("|",i)
	if vux("MSACD").exists() set lncradtl.msacd = vux("MSACD").piece("|",i)
	if vux("CRASTCD").exists() set lncradtl.crastcd = vux("CRASTCD").piece("|",i)
	if vux("CRACNTCD").exists() set lncradtl.cracntcd = vux("CRACNTCD").piece("|",i)
	if vux("CEN").exists() set lncradtl.cen = vux("CEN").piece("|",i)
	if vux("CID").exists() set lncradtl.cid = vux("CID").piece("|",i)
	quit
