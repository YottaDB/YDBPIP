PROCJFIL(RecordPROCESSJNL processjnl, String vpar, Boolean vparNorm) // PROCESSJNL - Process ID Journal Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 12/06/2007 16:14 - chenardp
	// Generated from DATA-QWIK schema in: /profile/v72framework_gtm  by: /v72framework_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (24)             02/21/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type Public Number ER = 0
	type Public String RM

	type public String verrors()

	catch fERROR {
		if fERROR.type="%PSL-E-DBFILER" do {
			set ER = 1
			set RM = fERROR.description
		}
		else  throw fERROR
	}
	type String vx(), vxins()                              // audit column array
	type Number %O = processjnl.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(processjnl,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(processjnl,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("SYSTEMDAT").exists()!vx("PID").exists()!vx("JNLSEQ").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("PROCESSJNL",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("PROCESSJNL","SYSTEMDAT = :processjnl.systemdat and PID = :processjnl.pid and JNLSEQ = :processjnl.jnlseq") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String SYSTEMDAT
	type public String PID
	type public String JNLSEQ

	type RecordPROCESSJNL processjnl = Db.getRecord("PROCESSJNL","SYSTEMDAT=:SYSTEMDAT,PID=:PID,JNLSEQ=:JNLSEQ")
	if (%ProcessMode = 2) do {
		do processjnl.setMode(2)

		do PROCJFIL(processjnl,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordPROCESSJNL processjnl

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("PROCESSJNL","SYSTEMDAT = :processjnl.systemdat and PID = :processjnl.pid and JNLSEQ = :processjnl.jnlseq") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(processjnl)) S ^PROCJNL(vobj(processjnl,-3),vobj(processjnl,-4),vobj(processjnl,-5))=vobj(processjnl)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordPROCESSJNL processjnl


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^PROCJNL(vobj(processjnl,-3),vobj(processjnl,-4),vobj(processjnl,-5))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordPROCESSJNL processjnl

	if processjnl.action.isNull() do vreqerr("ACTION") quit
	if processjnl.issdate.isNull() do vreqerr("ISSDATE") quit
	if processjnl.isspid.isNull() do vreqerr("ISSPID") quit
	if processjnl.isstime.isNull() do vreqerr("ISSTIME") quit
	if processjnl.isstlo.isNull() do vreqerr("ISSTLO") quit
	if processjnl.issusrnm.isNull() do vreqerr("ISSUSRNM") quit
	if processjnl.mode.isNull() do vreqerr("MODE") quit
	if processjnl.prctyp.isNull() do vreqerr("PRCTYP") quit
	if processjnl.regdate.isNull() do vreqerr("REGDATE") quit
	if processjnl.regtime.isNull() do vreqerr("REGTIME") quit
	if processjnl.status.isNull() do vreqerr("STATUS") quit
	if processjnl.tlo.isNull() do vreqerr("TLO") quit
	if processjnl.usrnam.isNull() do vreqerr("USRNAM") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordPROCESSJNL processjnl

	if processjnl.systemdat.isNull() do vreqerr("SYSTEMDAT") quit
	if processjnl.pid.isNull() do vreqerr("PID") quit
	if processjnl.jnlseq.isNull() do vreqerr("JNLSEQ") quit

		if vx("ACTION").exists(),processjnl.action.isNull() do vreqerr("ACTION") quit
		if vx("ISSDATE").exists(),processjnl.issdate.isNull() do vreqerr("ISSDATE") quit
		if vx("ISSPID").exists(),processjnl.isspid.isNull() do vreqerr("ISSPID") quit
		if vx("ISSTIME").exists(),processjnl.isstime.isNull() do vreqerr("ISSTIME") quit
		if vx("ISSTLO").exists(),processjnl.isstlo.isNull() do vreqerr("ISSTLO") quit
		if vx("ISSUSRNM").exists(),processjnl.issusrnm.isNull() do vreqerr("ISSUSRNM") quit
		if vx("MODE").exists(),processjnl.mode.isNull() do vreqerr("MODE") quit
		if vx("PRCTYP").exists(),processjnl.prctyp.isNull() do vreqerr("PRCTYP") quit
		if vx("REGDATE").exists(),processjnl.regdate.isNull() do vreqerr("REGDATE") quit
		if vx("REGTIME").exists(),processjnl.regtime.isNull() do vreqerr("REGTIME") quit
		if vx("STATUS").exists(),processjnl.status.isNull() do vreqerr("STATUS") quit
		if vx("TLO").exists(),processjnl.tlo.isNull() do vreqerr("TLO") quit
		if vx("USRNAM").exists(),processjnl.usrnam.isNull() do vreqerr("USRNAM") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("PROCESSJNL","MSG",1767,"PROCESSJNL."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordPROCESSJNL processjnl

	set X = processjnl.systemdat if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("SYSTEMDAT", vRM) quit
	set X = processjnl.pid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("PID", vRM) quit
	set X = processjnl.jnlseq if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("JNLSEQ", vRM) quit
	if processjnl.action.length()>12 set vRM = $$^MSG(1076,12) do vdderr("ACTION", vRM) quit
	if processjnl.event.length()>12 set vRM = $$^MSG(1076,12) do vdderr("EVENT", vRM) quit
	if processjnl.func.length()>12 set vRM = $$^MSG(1076,12) do vdderr("FUNC", vRM) quit
	set X = processjnl.issdate if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("ISSDATE", vRM) quit
	set X = processjnl.isspid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("ISSPID", vRM) quit
	set X = processjnl.isstime if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"C") do vdderr("ISSTIME", vRM) quit
	if processjnl.isstlo.length()>40 set vRM = $$^MSG(1076,40) do vdderr("ISSTLO", vRM) quit
	if processjnl.issusrnm.length()>40 set vRM = $$^MSG(1076,40) do vdderr("ISSUSRNM", vRM) quit
	if processjnl.mode.length()>12 set vRM = $$^MSG(1076,12) do vdderr("MODE", vRM) quit
	set X = processjnl.prcdate if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("PRCDATE", vRM) quit
	set X = processjnl.prctime if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"C") do vdderr("PRCTIME", vRM) quit
	if processjnl.prctyp.length()>10 set vRM = $$^MSG(1076,10) do vdderr("PRCTYP", vRM) quit
	if processjnl.qualif.length()>80 set vRM = $$^MSG(1076,80) do vdderr("QUALIF", vRM) quit
	set X = processjnl.regdate if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("REGDATE", vRM) quit
	set X = processjnl.regtime if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"C") do vdderr("REGTIME", vRM) quit
	if processjnl.status.length()>12 set vRM = $$^MSG(1076,12) do vdderr("STATUS", vRM) quit
	if processjnl.subtyp.length()>10 set vRM = $$^MSG(1076,10) do vdderr("SUBTYP", vRM) quit
	if processjnl.tlo.length()>40 set vRM = $$^MSG(1076,40) do vdderr("TLO", vRM) quit
	if processjnl.userid.length()>12 set vRM = $$^MSG(1076,12) do vdderr("USERID", vRM) quit
	if processjnl.usrcls.length()>12 set vRM = $$^MSG(1076,12) do vdderr("USRCLS", vRM) quit
	if processjnl.usrnam.length()>40 set vRM = $$^MSG(1076,40) do vdderr("USRNAM", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("PROCESSJNL","MSG",979,"PROCESSJNL."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordPROCESSJNL processjnl

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("SYSTEMDAT").exists() set vux("SYSTEMDAT") = vx("SYSTEMDAT")
	if vx("PID").exists() set vux("PID") = vx("PID")
	if vx("JNLSEQ").exists() set vux("JNLSEQ") = vx("JNLSEQ")
	do vkey(1) set voldkey = processjnl.systemdat_","_processjnl.pid_","_processjnl.jnlseq // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = processjnl.systemdat_","_processjnl.pid_","_processjnl.jnlseq // Copy new keys into object
	type RecordPROCESSJNL vnewrec = processjnl.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("PROCESSJNL",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordPROCESSJNL processjnl

	if vux("SYSTEMDAT").exists() set processjnl.systemdat = vux("SYSTEMDAT").piece("|",i)
	if vux("PID").exists() set processjnl.pid = vux("PID").piece("|",i)
	if vux("JNLSEQ").exists() set processjnl.jnlseq = vux("JNLSEQ").piece("|",i)
	quit
