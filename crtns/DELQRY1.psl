DELQRY1	// Delinquency queries for collection order stops compiled program
	// Last compiled:  06/19/2007 12:47 AM - 1369

	// THIS IS A COMPILED ROUTINE.  Compiled by procedure DELQRY

	quit   // Cannot access from top

SUPP()	// Suppress Collection Order1
	type RecordLN xobj1=Db.getRecord("LN","CID=:CID")
	if (xobj1.pmtdel>2&(xobj1.dpmt>xobj1.mbldelq))!(+xobj1.darcls=2)
	else  quit 0
	quit 1

SUPPRESS()	// Suppress Collection Order
	type RecordLN xobj1=Db.getRecord("LN","CID=:CID")
	if (xobj1.pmtdel>2&(xobj1.dpmt>xobj1.mbldelq))!(+xobj1.darcls=2)
	else  quit 0
	quit 1
