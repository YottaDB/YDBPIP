EVENTTFL(RecordEVENTTESTS eventtests, String vpar, Boolean vparNorm) // EVENTTESTS - Registered event tests Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 01/31/2007 11:24 - shetyes
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (4)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = eventtests.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar
	if %O = 0 do AUDIT^UCUTILN(eventtests,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(eventtests,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("TESTID").exists() do vkchged quit              // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("EVENTTESTS",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("EVENTTESTS","TESTID = :eventtests.testid") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String TESTID

	type RecordEVENTTESTS eventtests = Db.getRecord("EVENTTESTS","TESTID=:TESTID")
	if (%ProcessMode = 2) do {
		do eventtests.setMode(2)

		do EVENTTFL(eventtests,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordEVENTTESTS eventtests

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("EVENTTESTS","TESTID = :eventtests.testid") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBSFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(eventtests))  S ^EVENTINF("TESTS",vobj(eventtests,-3))=vobj(eventtests)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordEVENTTESTS eventtests

	if vpar["/CASDEL/" do VCASDEL                          // Cascade delete

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602
	ZWI ^EVENTINF("TESTS",vobj(eventtests,-3))             // Delete Record
	quit


vinit // Initialize default values

	type public RecordEVENTTESTS eventtests

	// Type local variables for access keys for defaults
	type public String TESTID

	if eventtests.issystemtest.isNull() set eventtests.issystemtest = 0 // issystemtest
	quit


vreqn // Validate required data items

	type public RecordEVENTTESTS eventtests

	if eventtests.issystemtest.isNull() do vreqerr("ISSYSTEMTEST") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordEVENTTESTS eventtests

	if eventtests.testid.isNull() do vreqerr("TESTID") quit

		if vx("ISSYSTEMTEST").exists(),eventtests.issystemtest.isNull() do vreqerr("ISSYSTEMTEST") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EVENTTESTS","MSG",1767,"EVENTTESTS."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordEVENTTESTS eventtests

	set X = eventtests.testid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TESTID", vRM) quit
	if eventtests.description.length()>40 set vRM = $$^MSG(1076,40) do vdderr("DESCRIPTION", vRM) quit
	if '("01"[eventtests.ISSYSTEMTEST) set vRM=$$^MSG(742,"L") do vdderr("ISSYSTEMTEST", vRM) quit
	if eventtests.procedure.length()>60 set vRM = $$^MSG(1076,60) do vdderr("PROCEDURE", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("EVENTTESTS","MSG",979,"EVENTTESTS."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordEVENTTESTS eventtests

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	set vux = vx("TESTID")
	set voldkey = vux.piece("|",1),vobj(eventtests,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(eventtests,-3) = vnewkey // Copy new keys into object
	type RecordEVENTTESTS vnewrec = eventtests.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("EVENTTESTS",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	set vobj(eventtests,-3) = vux.piece("|",1)             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

VCASDEL // Cascade delete logic

	type public RecordEVENTTESTS eventtests
	type public String vpar

	do Db.delete("EVENTARGDEFS","TESTID=:eventtests.testid",vpar) // Cascade delete

	do Db.delete("EVENTSAVAIL","ENTITYTESTID=:eventtests.testid",vpar) // Cascade delete

	do Db.delete("EVENTSAVAIL","SYSTEMTESTID=:eventtests.testid",vpar) // Cascade delete

	quit
