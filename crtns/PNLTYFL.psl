PNLTYFL(RecordPNLTYEVENT pnltyevent, String vpar, Boolean vparNorm) // PNLTYEVENT - Penalty Event and Balance Tracking Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:19 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (8)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = pnltyevent.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(pnltyevent,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(pnltyevent,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CID").exists()!vx("PDT").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("PNLTYEVENT",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("PNLTYEVENT","CID = :pnltyevent.cid and PDT = :pnltyevent.pdt") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CID
	type public String PDT

	type RecordPNLTYEVENT pnltyevent = Db.getRecord("PNLTYEVENT","CID=:CID,PDT=:PDT")
	if (%ProcessMode = 2) do {
		do pnltyevent.setMode(2)

		do PNLTYFL(pnltyevent,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordPNLTYEVENT pnltyevent

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("PNLTYEVENT","CID = :pnltyevent.cid and PDT = :pnltyevent.pdt") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(pnltyevent)) S ^PNLTYEV(vobj(pnltyevent,-3),vobj(pnltyevent,-4))=vobj(pnltyevent)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordPNLTYEVENT pnltyevent


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^PNLTYEV(vobj(pnltyevent,-3),vobj(pnltyevent,-4))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordPNLTYEVENT pnltyevent

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordPNLTYEVENT pnltyevent

	if pnltyevent.cid.isNull() do vreqerr("CID") quit
	if pnltyevent.pdt.isNull() do vreqerr("PDT") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("PNLTYEVENT","MSG",1767,"PNLTYEVENT."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordPNLTYEVENT pnltyevent
	if 'pnltyevent.cid.isNull() set vfkey("^ACN("_""""_pnltyevent.cid_""""_","_50_")")="PNLTYEVENT(CID) -> ACN"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordPNLTYEVENT pnltyevent
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("ACN",":pnltyevent.cid") set vERRMSG = $$^MSG(8563,"PNLTYEVENT(CID) -> ACN") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordPNLTYEVENT pnltyevent

	set X = pnltyevent.cid if 'X.isNull(),'Db.isDefined("ACN","X") set vRM = $$^MSG(1485,X) do vdderr("CID", vRM) quit
	set X = pnltyevent.pdt if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("PDT", vRM) quit
	set X = pnltyevent.evbal1 if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"PNLTYEVENT.EVBAL1"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = pnltyevent.evbal2 if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"PNLTYEVENT.EVBAL2"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = pnltyevent.evbal3 if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"PNLTYEVENT.EVBAL3"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = pnltyevent.evbal4 if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"PNLTYEVENT.EVBAL4"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = pnltyevent.evcode if 'X.isNull(),'Db.isDefined("STBLPEVENT","X") set vRM = $$^MSG(1485,X) do vdderr("EVCODE", vRM) quit
	set X = pnltyevent.penbal if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"PNLTYEVENT.PENBAL"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("PNLTYEVENT","MSG",979,"PNLTYEVENT."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordPNLTYEVENT pnltyevent

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CID").exists() set vux("CID") = vx("CID")
	if vx("PDT").exists() set vux("PDT") = vx("PDT")
	do vkey(1) set voldkey = pnltyevent.cid_","_pnltyevent.pdt // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = pnltyevent.cid_","_pnltyevent.pdt // Copy new keys into object
	type RecordPNLTYEVENT vnewrec = pnltyevent.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("PNLTYEVENT",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordPNLTYEVENT pnltyevent

	if vux("CID").exists() set pnltyevent.cid = vux("CID").piece("|",i)
	if vux("PDT").exists() set pnltyevent.pdt = vux("PDT").piece("|",i)
	quit
