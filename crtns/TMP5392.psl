TMP5392	// DBSEDIT temporary filer compiled program
	// Last compiled:  04/04/2007 01:21 AM - lc6990

	// THIS IS A COMPILED ROUTINE.  Compiled by procedure DBSEDIT

	quit:(%ProcessID '= 15392)
	type RecordUTBLIRADIS rec = Db.getRecord("UTBLIRADIS","KEY=""159""",1)
	do rec.setAuditFlag(1)
	set rec.DESC = "Death & excess contr current year (Rpr)"
	set rec.RSN = "60"
	set rec.WH = "$$WHT1^IRAWH"
	set rec.AGE = "0"
	set rec.PERIOD = "0"
	set rec.LIF = "0"
	do ^DBSLOGIT(rec, rec.getMode())
	do rec.save()
	quit
