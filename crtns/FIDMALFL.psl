FIDMALFL(RecordFIDMALL fidmall, String vpar, Boolean vparNorm) // FIDMALL - FIDM All Accounts Reportable Data Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:16 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (13)             08/11/2005
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = fidmall.getMode()                     // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(fidmall,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(fidmall,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("REPDATE").exists()!vx("REPTYPE").exists()!vx("TAXIDPR").exists()!vx("CID").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("FIDMALL",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("FIDMALL","REPDATE = :fidmall.repdate and REPTYPE = :fidmall.reptype and TAXIDPR = :fidmall.taxidpr and CID = :fidmall.cid") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String REPDATE
	type public String REPTYPE
	type public String TAXIDPR
	type public String CID

	type RecordFIDMALL fidmall = Db.getRecord("FIDMALL","REPDATE=:REPDATE,REPTYPE=:REPTYPE,TAXIDPR=:TAXIDPR,CID=:CID")
	if (%ProcessMode = 2) do {
		do fidmall.setMode(2)

		do FIDMALFL(fidmall,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordFIDMALL fidmall

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("FIDMALL","REPDATE = :fidmall.repdate and REPTYPE = :fidmall.reptype and TAXIDPR = :fidmall.taxidpr and CID = :fidmall.cid") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(fidmall)) S ^FIDMALL(vobj(fidmall,-3),vobj(fidmall,-4),vobj(fidmall,-5),vobj(fidmall,-6))=vobj(fidmall)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordFIDMALL fidmall


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^FIDMALL(vobj(fidmall,-3),vobj(fidmall,-4),vobj(fidmall,-5),vobj(fidmall,-6))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordFIDMALL fidmall

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordFIDMALL fidmall

	if fidmall.repdate.isNull() do vreqerr("REPDATE") quit
	if fidmall.reptype.isNull() do vreqerr("REPTYPE") quit
	if fidmall.taxidpr.isNull() do vreqerr("TAXIDPR") quit
	if fidmall.cid.isNull() do vreqerr("CID") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("FIDMALL","MSG",1767,"FIDMALL."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordFIDMALL fidmall

	set X = fidmall.repdate if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("REPDATE", vRM) quit
	set X = fidmall.reptype if 'X.isNull(),'Db.isDefined("STBLFIDM","X") set vRM = $$^MSG(1485,X) do vdderr("REPTYPE", vRM) quit
	if fidmall.taxidpr.length()>20 set vRM = $$^MSG(1076,20) do vdderr("TAXIDPR", vRM) quit
	set X = fidmall.cid if 'X.isNull(),'Db.isDefined("ACN","X") set vRM = $$^MSG(1485,X) do vdderr("CID", vRM) quit
	set X = fidmall.acctind if 'X.isNull(),X'?1N set vRM=$$^MSG(742,"N") do vdderr("ACCTIND", vRM) quit
	set X = fidmall.acn if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("ACN", vRM) quit
	set X = fidmall.bal if 'X.isNull() set vRM = $$VAL^DBSVER("$",18,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"FIDMALL.BAL"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = fidmall.cidtype if 'X.isNull(),X'?1.2N,X'?1"-"1.1N set vRM=$$^MSG(742,"N") do vdderr("CIDTYPE", vRM) quit
	if fidmall.prowner.length()>40 set vRM = $$^MSG(1076,40) do vdderr("PROWNER", vRM) quit
	if fidmall.secowner.length()>40 set vRM = $$^MSG(1076,40) do vdderr("SECOWNER", vRM) quit
	if fidmall.taxidsec.length()>20 set vRM = $$^MSG(1076,20) do vdderr("TAXIDSEC", vRM) quit
	set X = fidmall.trustind if 'X.isNull(),X'?1N set vRM=$$^MSG(742,"N") do vdderr("TRUSTIND", vRM) quit
	set X = fidmall.updind if 'X.isNull(),X'?1N set vRM=$$^MSG(742,"N") do vdderr("UPDIND", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("FIDMALL","MSG",979,"FIDMALL."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordFIDMALL fidmall

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("REPDATE").exists() set vux("REPDATE") = vx("REPDATE")
	if vx("REPTYPE").exists() set vux("REPTYPE") = vx("REPTYPE")
	if vx("TAXIDPR").exists() set vux("TAXIDPR") = vx("TAXIDPR")
	if vx("CID").exists() set vux("CID") = vx("CID")
	do vkey(1) set voldkey = fidmall.repdate_","_fidmall.reptype_","_fidmall.taxidpr_","_fidmall.cid // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = fidmall.repdate_","_fidmall.reptype_","_fidmall.taxidpr_","_fidmall.cid // Copy new keys into object
	type RecordFIDMALL vnewrec = fidmall.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("FIDMALL",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordFIDMALL fidmall

	if vux("REPDATE").exists() set fidmall.repdate = vux("REPDATE").piece("|",i)
	if vux("REPTYPE").exists() set fidmall.reptype = vux("REPTYPE").piece("|",i)
	if vux("TAXIDPR").exists() set fidmall.taxidpr = vux("TAXIDPR").piece("|",i)
	if vux("CID").exists() set fidmall.cid = vux("CID").piece("|",i)
	quit
