CKREGFIL(RecordCHKREG1 chkreg1, String vpar, Boolean vparNorm) // CHKREG1 - Check Register Checks Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:13 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (16)             11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = chkreg1.getMode()                     // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(chkreg1,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(chkreg1,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CO").exists()!vx("GL").exists()!vx("CKNO").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("CHKREG1",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("CHKREG1","CO = :chkreg1.co and GL = :chkreg1.gl and CKNO = :chkreg1.ckno") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CO
	type public String GL
	type public String CKNO

	type RecordCHKREG1 chkreg1 = Db.getRecord("CHKREG1","CO=:CO,GL=:GL,CKNO=:CKNO")
	if (%ProcessMode = 2) do {
		do chkreg1.setMode(2)

		do CKREGFIL(chkreg1,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordCHKREG1 chkreg1

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("CHKREG1","CO = :chkreg1.co and GL = :chkreg1.gl and CKNO = :chkreg1.ckno") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(chkreg1)) S ^CHKREG(vobj(chkreg1,-3),vobj(chkreg1,-4),vobj(chkreg1,-5))=vobj(chkreg1)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordCHKREG1 chkreg1


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^CHKREG(vobj(chkreg1,-3),vobj(chkreg1,-4),vobj(chkreg1,-5))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordCHKREG1 chkreg1

	// Type local variables for access keys for defaults
	type public String CO
	type public String GL
	type public String CKNO

	if chkreg1.combined.isNull() set chkreg1.combined = 0  // combined
	quit


vreqn // Validate required data items

	type public RecordCHKREG1 chkreg1

	if chkreg1.combined.isNull() do vreqerr("COMBINED") quit
	if chkreg1.tjd.isNull() do vreqerr("TJD") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordCHKREG1 chkreg1

	if chkreg1.co.isNull() do vreqerr("CO") quit
	if chkreg1.gl.isNull() do vreqerr("GL") quit
	if chkreg1.ckno.isNull() do vreqerr("CKNO") quit

		if vx("COMBINED").exists(),chkreg1.combined.isNull() do vreqerr("COMBINED") quit
		if vx("TJD").exists(),chkreg1.tjd.isNull() do vreqerr("TJD") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CHKREG1","MSG",1767,"CHKREG1."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordCHKREG1 chkreg1

	if chkreg1.co.length()>12 set vRM = $$^MSG(1076,12) do vdderr("CO", vRM) quit
	set X = chkreg1.gl if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("GL", vRM) quit
	set X = chkreg1.ckno if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("CKNO", vRM) quit
	set X = chkreg1.brcd if 'X.isNull(),'Db.isDefined("UTBLBRCD","X") set vRM = $$^MSG(1485,X) do vdderr("BRCD", vRM) quit
	set X = chkreg1.cid if 'X.isNull(),'Db.isDefined("ACN","X") set vRM = $$^MSG(1485,X) do vdderr("CID", vRM) quit
	set X = chkreg1.clear if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("CLEAR", vRM) quit
	if '("01"[chkreg1.COMBINED) set vRM=$$^MSG(742,"L") do vdderr("COMBINED", vRM) quit
	set X = chkreg1.ctype if 'X.isNull(),'Db.isDefined("UTBLCHKS","X") set vRM = $$^MSG(1485,X) do vdderr("CTYPE", vRM) quit
	set X = chkreg1.efd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("EFD", vRM) quit
	if chkreg1.memo.length()>40 set vRM = $$^MSG(1076,40) do vdderr("MEMO", vRM) quit
	if chkreg1.payee.length()>40 set vRM = $$^MSG(1076,40) do vdderr("PAYEE", vRM) quit
	set X = chkreg1.status if 'X.isNull(),'Db.isDefined("STBLCHKSTS","X") set vRM = $$^MSG(1485,X) do vdderr("STATUS", vRM) quit
	set X = chkreg1.tamt if 'X.isNull() set vRM = $$VAL^DBSVER("$",12,0,,,,,2) if 'vRM.isNull() set vRM = $$^MSG(979,"CHKREG1.TAMT"_" "_vRM) throw Class.new("Error","%PSL-E-DBFILER,"_vRM.replace(",","~"))
	set X = chkreg1.tjd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("TJD", vRM) quit
	set X = chkreg1.trc if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("TRC", vRM) quit
	set X = chkreg1.uid if 'X.isNull(),'Db.isDefined("SCAU","X") set vRM = $$^MSG(1485,X) do vdderr("UID", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("CHKREG1","MSG",979,"CHKREG1."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordCHKREG1 chkreg1

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CO").exists() set vux("CO") = vx("CO")
	if vx("GL").exists() set vux("GL") = vx("GL")
	if vx("CKNO").exists() set vux("CKNO") = vx("CKNO")
	do vkey(1) set voldkey = chkreg1.co_","_chkreg1.gl_","_chkreg1.ckno // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = chkreg1.co_","_chkreg1.gl_","_chkreg1.ckno // Copy new keys into object
	type RecordCHKREG1 vnewrec = chkreg1.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("CHKREG1",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordCHKREG1 chkreg1

	if vux("CO").exists() set chkreg1.co = vux("CO").piece("|",i)
	if vux("GL").exists() set chkreg1.gl = vux("GL").piece("|",i)
	if vux("CKNO").exists() set chkreg1.ckno = vux("CKNO").piece("|",i)
	quit
