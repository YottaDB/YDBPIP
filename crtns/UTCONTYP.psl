UTCONTYP(RecordUTBLCONTYP utblcontyp, String vpar, Boolean vparNorm) // UTBLCONTYP - Contribution Codes Based on Plan Type Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (3)              06/05/2007
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblcontyp.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utblcontyp,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utblcontyp,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("IRATYP").exists()!vx("CON").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLCONTYP",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLCONTYP","IRATYP = :utblcontyp.iratyp and CON = :utblcontyp.con") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String IRATYP
	type public String CON

	type RecordUTBLCONTYP utblcontyp = Db.getRecord("UTBLCONTYP","IRATYP=:IRATYP,CON=:CON")
	if (%ProcessMode = 2) do {
		do utblcontyp.setMode(2)

		do UTCONTYP(utblcontyp,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLCONTYP utblcontyp

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLCONTYP","IRATYP = :utblcontyp.iratyp and CON = :utblcontyp.con") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblcontyp,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblcontyp,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblcontyp)) S ^UTBL("CONTYP",vobj(utblcontyp,-3),vobj(utblcontyp,-4))=vobj(utblcontyp)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLCONTYP utblcontyp

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblcontyp,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^UTBL("CONTYP",vobj(utblcontyp,-3),vobj(utblcontyp,-4))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordUTBLCONTYP utblcontyp

	if utblcontyp.fmdesc.isNull() do vreqerr("FMDESC") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLCONTYP utblcontyp

	if utblcontyp.iratyp.isNull() do vreqerr("IRATYP") quit
	if utblcontyp.con.isNull() do vreqerr("CON") quit

		if vx("FMDESC").exists(),utblcontyp.fmdesc.isNull() do vreqerr("FMDESC") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLCONTYP","MSG",1767,"UTBLCONTYP."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLCONTYP utblcontyp

	set X = utblcontyp.iratyp if 'X.isNull(),'Db.isDefined("UTBLRSPDEF","X") set vRM = $$^MSG(1485,X) do vdderr("IRATYP", vRM) quit
	set X = utblcontyp.con if 'X.isNull(),'Db.isDefined("UTBLIRACON","X") set vRM = $$^MSG(1485,X) do vdderr("CON", vRM) quit
	if utblcontyp.fmdesc.length()>40 set vRM = $$^MSG(1076,40) do vdderr("FMDESC", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLCONTYP","MSG",979,"UTBLCONTYP."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLCONTYP utblcontyp

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("IRATYP").exists() set vux("IRATYP") = vx("IRATYP")
	if vx("CON").exists() set vux("CON") = vx("CON")
	do vkey(1) set voldkey = utblcontyp.iratyp_","_utblcontyp.con // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = utblcontyp.iratyp_","_utblcontyp.con // Copy new keys into object
	type RecordUTBLCONTYP vnewrec = utblcontyp.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLCONTYP",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordUTBLCONTYP utblcontyp

	if vux("IRATYP").exists() set utblcontyp.iratyp = vux("IRATYP").piece("|",i)
	if vux("CON").exists() set utblcontyp.con = vux("CON").piece("|",i)
	quit
