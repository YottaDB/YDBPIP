MSGLSQFL(RecordMSGLOGSEQ msglogseq, String vpar, Boolean vparNorm) // MSGLOGSEQ - Client Messages/Server Replies Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 01/31/2007 11:22 - shetyes
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (3)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = msglogseq.getMode()                   // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar
	if %O = 0 do AUDIT^UCUTILN(msglogseq,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(msglogseq,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("TOKEN").exists()!vx("MSGID").exists()!vx("MSGTYP").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("MSGLOGSEQ",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("MSGLOGSEQ","TOKEN = :msglogseq.token and MSGID = :msglogseq.msgid and MSGTYP = :msglogseq.msgtyp") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String TOKEN
	type public String MSGID
	type public String MSGTYP

	type RecordMSGLOGSEQ msglogseq = Db.getRecord("MSGLOGSEQ","TOKEN=:TOKEN,MSGID=:MSGID,MSGTYP=:MSGTYP")
	if (%ProcessMode = 2) do {
		do msglogseq.setMode(2)

		do MSGLSQFL(msglogseq,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordMSGLOGSEQ msglogseq

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("MSGLOGSEQ","TOKEN = :msglogseq.token and MSGID = :msglogseq.msgid and MSGTYP = :msglogseq.msgtyp") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBSFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(msglogseq))  K:$D(vobj(msglogseq,1,1)) ^MSGLOG(vobj(msglogseq,-3),vobj(msglogseq,-4),vobj(msglogseq,-5)) S ^MSGLOG(vobj(msglogseq,-3),vobj(msglogseq,-4),vobj(msglogseq,-5))=vobj(msglogseq)
		#ENDBYPASS
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
			if $D(vobj(msglogseq,1,1))  N vS1,vS2 S vS1=0 F vS2=1:450:$L(vobj(msglogseq,1,1)) S vS1=vS1+1,^MSGLOG(vobj(msglogseq,-3),vobj(msglogseq,-4),vobj(msglogseq,-5),vS1)=$E(vobj(msglogseq,1,1),vS2,vS2+449)
			#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordMSGLOGSEQ msglogseq


	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602
	kill ^MSGLOG(vobj(msglogseq,-3),vobj(msglogseq,-4),vobj(msglogseq,-5)) // Delete Record
	quit


vreqn // Validate required data items

	type public RecordMSGLOGSEQ msglogseq

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordMSGLOGSEQ msglogseq

	if msglogseq.token.isNull() do vreqerr("TOKEN") quit
	if msglogseq.msgid.isNull() do vreqerr("MSGID") quit
	if msglogseq.msgtyp.isNull() do vreqerr("MSGTYP") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("MSGLOGSEQ","MSG",1767,"MSGLOGSEQ."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordMSGLOGSEQ msglogseq
	if 'msglogseq.msgid.isNull() set vfkey("^MSGLOG("_""""_msglogseq.token_""""_","_""""_msglogseq.msgid_""""_")")="MSGLOGSEQ(TOKEN,MSGID) -> MSGLOG"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordMSGLOGSEQ msglogseq
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("MSGLOG",":msglogseq.token,:msglogseq.msgid") set vERRMSG = $$^MSG(8563,"MSGLOGSEQ(TOKEN,MSGID) -> MSGLOG") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordMSGLOGSEQ msglogseq

	if msglogseq.token.length()>10 set vRM = $$^MSG(1076,10) do vdderr("TOKEN", vRM) quit
	if msglogseq.msgid.length()>40 set vRM = $$^MSG(1076,40) do vdderr("MSGID", vRM) quit
	set X = msglogseq.msgtyp if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("MSGTYP", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("MSGLOGSEQ","MSG",979,"MSGLOGSEQ."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordMSGLOGSEQ msglogseq

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("TOKEN").exists() set vux("TOKEN") = vx("TOKEN")
	if vx("MSGID").exists() set vux("MSGID") = vx("MSGID")
	if vx("MSGTYP").exists() set vux("MSGTYP") = vx("MSGTYP")
	do vkey(1) set voldkey = msglogseq.token_","_msglogseq.msgid_","_msglogseq.msgtyp // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = msglogseq.token_","_msglogseq.msgid_","_msglogseq.msgtyp // Copy new keys into object
	type RecordMSGLOGSEQ vnewrec = msglogseq.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("MSGLOGSEQ",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordMSGLOGSEQ msglogseq

	if vux("TOKEN").exists() set msglogseq.token = vux("TOKEN").piece("|",i)
	if vux("MSGID").exists() set msglogseq.msgid = vux("MSGID").piece("|",i)
	if vux("MSGTYP").exists() set msglogseq.msgtyp = vux("MSGTYP").piece("|",i)
	quit
