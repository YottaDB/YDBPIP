DELUXCFL(RecordUTBLDELUXE utbldeluxe, String vpar, Boolean vparNorm) // UTBLDELUXE - Deluxe Check Ordering Options - Company Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (6)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utbldeluxe.getMode()                  // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utbldeluxe,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utbldeluxe,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CO").exists() do vkchged quit                  // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLDELUXE",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLDELUXE","CO = :utbldeluxe.co") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CO

	type RecordUTBLDELUXE utbldeluxe = Db.getRecord("UTBLDELUXE","CO=:CO")
	if (%ProcessMode = 2) do {
		do utbldeluxe.setMode(2)

		do DELUXCFL(utbldeluxe,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLDELUXE utbldeluxe

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLDELUXE","CO = :utbldeluxe.co") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utbldeluxe,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utbldeluxe,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utbldeluxe)) S ^UTBL("DELUXE",vobj(utbldeluxe,-3))=vobj(utbldeluxe)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLDELUXE utbldeluxe

	if vpar'["/NOLOG/" do ^DBSLOGIT(utbldeluxe,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^UTBL("DELUXE",vobj(utbldeluxe,-3))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordUTBLDELUXE utbldeluxe

	// Type local variables for access keys for defaults
	type public String CO

	if utbldeluxe.ibrcd.isNull() set utbldeluxe.ibrcd = 0  // ibrcd
	if utbldeluxe.ihph.isNull() set utbldeluxe.ihph = 0    // ihph
	if utbldeluxe.iod.isNull() set utbldeluxe.iod = 0      // iod
	if utbldeluxe.itaxid.isNull() set utbldeluxe.itaxid = 0 // itaxid
	quit


vreqn // Validate required data items

	type public RecordUTBLDELUXE utbldeluxe

	if utbldeluxe.ibrcd.isNull() do vreqerr("IBRCD") quit
	if utbldeluxe.ihph.isNull() do vreqerr("IHPH") quit
	if utbldeluxe.iod.isNull() do vreqerr("IOD") quit
	if utbldeluxe.itaxid.isNull() do vreqerr("ITAXID") quit
	if utbldeluxe.rec.isNull() do vreqerr("REC") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLDELUXE utbldeluxe

	if utbldeluxe.co.isNull() do vreqerr("CO") quit

		if vx("IBRCD").exists(),utbldeluxe.ibrcd.isNull() do vreqerr("IBRCD") quit
		if vx("IHPH").exists(),utbldeluxe.ihph.isNull() do vreqerr("IHPH") quit
		if vx("IOD").exists(),utbldeluxe.iod.isNull() do vreqerr("IOD") quit
		if vx("ITAXID").exists(),utbldeluxe.itaxid.isNull() do vreqerr("ITAXID") quit
		if vx("REC").exists(),utbldeluxe.rec.isNull() do vreqerr("REC") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLDELUXE","MSG",1767,"UTBLDELUXE."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLDELUXE utbldeluxe

	set X = utbldeluxe.co if 'X.isNull(),'Db.isDefined("UTBLCO","X") set vRM = $$^MSG(1485,X) do vdderr("CO", vRM) quit
	if '("01"[utbldeluxe.IBRCD) set vRM=$$^MSG(742,"L") do vdderr("IBRCD", vRM) quit
	if '("01"[utbldeluxe.IHPH) set vRM=$$^MSG(742,"L") do vdderr("IHPH", vRM) quit
	if '("01"[utbldeluxe.IOD) set vRM=$$^MSG(742,"L") do vdderr("IOD", vRM) quit
	if '("01"[utbldeluxe.ITAXID) set vRM=$$^MSG(742,"L") do vdderr("ITAXID", vRM) quit
	if utbldeluxe.rec.length()>50 set vRM = $$^MSG(1076,50) do vdderr("REC", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLDELUXE","MSG",979,"UTBLDELUXE."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLDELUXE utbldeluxe

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	set vux = vx("CO")
	set voldkey = vux.piece("|",1),vobj(utbldeluxe,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(utbldeluxe,-3) = vnewkey // Copy new keys into object
	type RecordUTBLDELUXE vnewrec = utbldeluxe.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLDELUXE",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	set vobj(utbldeluxe,-3) = vux.piece("|",1)             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit
