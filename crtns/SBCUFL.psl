SBCUFL(RecordUTBLSBCU utblsbcu, String vpar, Boolean vparNorm) // UTBLSBCU - Shared Branch Network Credit Unions Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:21 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (9)              11/14/2005
	// Trigger Definition (2)                      09/21/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblsbcu.getMode()                    // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(utblsbcu,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utblsbcu,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	// Define local variables for access keys for legacy triggers
	type String ISOBIN = utblsbcu.isobin

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
		if vpar["/TRIGAFT/" do VAI                            // After insert triggers
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("ISOBIN").exists() do vkchged quit              // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLSBCU",.vx)
		set %O = 1 do vexec
		if vpar["/TRIGAFT/" do VAU                            // After update triggers
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
		if vpar["/TRIGAFT/" do VAI                            // After insert triggers
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLSBCU","ISOBIN = :utblsbcu.isobin") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String ISOBIN

	type RecordUTBLSBCU utblsbcu = Db.getRecord("UTBLSBCU","ISOBIN=:ISOBIN")
	if (%ProcessMode = 2) do {
		do utblsbcu.setMode(2)

		do SBCUFL(utblsbcu,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLSBCU utblsbcu

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLSBCU","ISOBIN = :utblsbcu.isobin") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblsbcu,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblsbcu,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblsbcu)) S ^UTBL("SBCU",vobj(utblsbcu,-3))=vobj(utblsbcu)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLSBCU utblsbcu

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblsbcu,3)

	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	ZWI ^UTBL("SBCU",vobj(utblsbcu,-3))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordUTBLSBCU utblsbcu

	// Type local variables for access keys for defaults
	type public String ISOBIN

	if utblsbcu.cntry.isNull() set utblsbcu.cntry = CUVAR.CNTRY // cntry
	quit


vreqn // Validate required data items

	type public RecordUTBLSBCU utblsbcu

	if utblsbcu.addr.isNull() do vreqerr("ADDR") quit
	if utblsbcu.city.isNull() do vreqerr("CITY") quit
	if utblsbcu.cntry.isNull() do vreqerr("CNTRY") quit
	if utblsbcu.nam.isNull() do vreqerr("NAM") quit
	if utblsbcu.rtngno.isNull() do vreqerr("RTNGNO") quit
	if utblsbcu.state.isNull() do vreqerr("STATE") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLSBCU utblsbcu

	if utblsbcu.isobin.isNull() do vreqerr("ISOBIN") quit

		if vx("ADDR").exists(),utblsbcu.addr.isNull() do vreqerr("ADDR") quit
		if vx("CITY").exists(),utblsbcu.city.isNull() do vreqerr("CITY") quit
		if vx("CNTRY").exists(),utblsbcu.cntry.isNull() do vreqerr("CNTRY") quit
		if vx("NAM").exists(),utblsbcu.nam.isNull() do vreqerr("NAM") quit
		if vx("RTNGNO").exists(),utblsbcu.rtngno.isNull() do vreqerr("RTNGNO") quit
		if vx("STATE").exists(),utblsbcu.state.isNull() do vreqerr("STATE") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLSBCU","MSG",1767,"UTBLSBCU."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VAI //
	type public Number ER = 0
	type public String vx(),RM
	do vai1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit

VAU //
	type public Number ER = 0
	type public String vx(),RM
	if vx("").order().isNull() quit
	if vx("CUZIP").exists() do vau1 if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vai1 // Trigger AFTER_INSERT - After Insert trigers

	type public RecordUTBLSBCU utblsbcu
	/*
	---- Revision History--------------------------------------------------------

	08/21/06 - KumarSS - 22169
		   Initial Revision.
		   This trigger validates ZIP code column of the credit union 
		   participating in the shared branch network.
	-----------------------------------------------------------------------------
	*/

	type public Boolean ER
	type public String RM

	// Validate ZIP code - Credit Union Participating the Shared Branck Network
	do ZIP^PROCZIP(utblsbcu.cntry, utblsbcu.state, utblsbcu.cuzip)
	if ER.get() do Runtime.setErrMSG("UTBLSBCU", 3333, RM)

	quit

	

vau1 // Trigger AU_CUZIP - After Update of Zip Code

	type public RecordUTBLSBCU utblsbcu

	/*
	--Revision History-----------------------------------------------------------

	08/21/06 - KumarSS - 22169
		   Initial Revision.
		   This trigger validates ZIP code column of the credit union 
		   participating in the shared branch network.
	-----------------------------------------------------------------------------
	*/

	type public Boolean ER
	type public String RM

	// Validate ZIP code - Credit Union Participating the Shared Branck Network
	do ZIP^PROCZIP(utblsbcu.cntry, utblsbcu.state, utblsbcu.cuzip)
	if ER.get() do Runtime.setErrMSG("UTBLSBCU", 3333, RM)

	quit


vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLSBCU utblsbcu

	if utblsbcu.isobin.length()>16 set vRM = $$^MSG(1076,16) do vdderr("ISOBIN", vRM) quit
	if utblsbcu.addr.length()>40 set vRM = $$^MSG(1076,40) do vdderr("ADDR", vRM) quit
	if utblsbcu.city.length()>40 set vRM = $$^MSG(1076,40) do vdderr("CITY", vRM) quit
	set X = utblsbcu.cntry if 'X.isNull(),'Db.isDefined("STBLCNTRY","X") set vRM = $$^MSG(1485,X) do vdderr("CNTRY", vRM) quit
	if utblsbcu.cuzip.length()>10 set vRM = $$^MSG(1076,10) do vdderr("CUZIP", vRM) quit
	if utblsbcu.nam.length()>40 set vRM = $$^MSG(1076,40) do vdderr("NAM", vRM) quit
	if utblsbcu.phone.length()>12 set vRM = $$^MSG(1076,12) do vdderr("PHONE", vRM) quit
	set X = utblsbcu.rtngno if 'X.isNull(),'Db.isDefined("UTBLRT","X") set vRM = $$^MSG(1485,X) do vdderr("RTNGNO", vRM) quit
	if utblsbcu.state.length()>2 set vRM = $$^MSG(1076,2) do vdderr("STATE", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLSBCU","MSG",979,"UTBLSBCU."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLSBCU utblsbcu

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux
	type String voldpar = vpar.get()                       // Save filer switches

	set vux = vx("ISOBIN")
	set voldkey = vux.piece("|",1),vobj(utblsbcu,-3) = voldkey // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	set vnewkey = vux.piece("|",2),vobj(utblsbcu,-3) = vnewkey // Copy new keys into object
	type RecordUTBLSBCU vnewrec = utblsbcu.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLSBCU",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update
	set vpar = voldpar
	if vpar["/TRIGAFT/" do VAU

	set vobj(utblsbcu,-3) = vux.piece("|",1)               // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit
