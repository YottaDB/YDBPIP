FILEREL1(RecordUTBLRELMAT1 utblrelmat1, String vpar, Boolean vparNorm) // UTBLRELMAT1 - Customer Relationship Status Matrix 1D Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 01/31/2007 11:25 - shetyes
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (3)              11/22/2003
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = utblrelmat1.getMode()                 // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar
	if %O = 0 do AUDIT^UCUTILN(utblrelmat1,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(utblrelmat1,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("MATRIX").exists()!vx("MATROW").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("UTBLRELMAT1",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("UTBLRELMAT1","MATRIX = :utblrelmat1.matrix and MATROW = :utblrelmat1.matrow") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String MATRIX
	type public String MATROW

	type RecordUTBLRELMAT1 utblrelmat1 = Db.getRecord("UTBLRELMAT1","MATRIX=:MATRIX,MATROW=:MATROW")
	if (%ProcessMode = 2) do {
		do utblrelmat1.setMode(2)

		do FILEREL1(utblrelmat1,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordUTBLRELMAT1 utblrelmat1

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("UTBLRELMAT1","MATRIX = :utblrelmat1.matrix and MATROW = :utblrelmat1.matrow") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBSFILER,"_vERRMSG.replace(",","~"))
	if vpar["/VALFK/" do CHKFKS                            // Check foreign keys
	if vpar["/VALRI/" do VFKEYS                            // Foreign key definition

	if vpar'["/NOUPDATE/" do {

		if %O = 0, vpar'["/NOLOG/" do ^DBSLOGIT(utblrelmat1,%O,.vxins())
		if %O = 1, vpar'["/NOLOG/" do ^DBSLOGIT(utblrelmat1,%O,.vx())

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(utblrelmat1))  S ^UTBL("RELMAT",vobj(utblrelmat1,-3),vobj(utblrelmat1,-4))=vobj(utblrelmat1)
		#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordUTBLRELMAT1 utblrelmat1

	if vpar'["/NOLOG/" do ^DBSLOGIT(utblrelmat1,3)

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602
	ZWI ^UTBL("RELMAT",vobj(utblrelmat1,-3),vobj(utblrelmat1,-4)) // Delete Record
	quit


vreqn // Validate required data items

	type public RecordUTBLRELMAT1 utblrelmat1

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordUTBLRELMAT1 utblrelmat1

	if utblrelmat1.matrix.isNull() do vreqerr("MATRIX") quit
	if utblrelmat1.matrow.isNull() do vreqerr("MATROW") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLRELMAT1","MSG",1767,"UTBLRELMAT1."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


VFKEYS // Foreign keys

	type public String vfkey(),vpar

	type public RecordUTBLRELMAT1 utblrelmat1
	if 'utblrelmat1.matrix.isNull() set vfkey("^UTBL("_"""RELMAT"""_","_""""_utblrelmat1.matrix_""""_")")="UTBLRELMAT1(MATRIX) -> UTBLRELMAT"
	quit

CHKFKS   // Check foreign keys when not under buffer


	type public RecordUTBLRELMAT1 utblrelmat1
	type public Number %O
	type String vERRMSG

	if 'Db.isDefined("UTBLRELMAT",":utblrelmat1.matrix") set vERRMSG = $$^MSG(8563,"UTBLRELMAT1(MATRIX) -> UTBLRELMAT") throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordUTBLRELMAT1 utblrelmat1

	set X = utblrelmat1.matrix if 'X.isNull(),'Db.isDefined("UTBLRELMAT","X") set vRM = $$^MSG(1485,X) do vdderr("MATRIX", vRM) quit
	if utblrelmat1.matrow.length()>20 set vRM = $$^MSG(1076,20) do vdderr("MATROW", vRM) quit
	set X = utblrelmat1.custstat if 'X.isNull(),'Db.isDefined("UTBLRELSTAT","X") set vRM = $$^MSG(1485,X) do vdderr("CUSTSTAT", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("UTBLRELMAT1","MSG",979,"UTBLRELMAT1."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordUTBLRELMAT1 utblrelmat1

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("MATRIX").exists() set vux("MATRIX") = vx("MATRIX")
	if vx("MATROW").exists() set vux("MATROW") = vx("MATROW")
	do vkey(1) set voldkey = utblrelmat1.matrix_","_utblrelmat1.matrow // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = utblrelmat1.matrix_","_utblrelmat1.matrow // Copy new keys into object
	type RecordUTBLRELMAT1 vnewrec = utblrelmat1.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("UTBLRELMAT1",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordUTBLRELMAT1 utblrelmat1

	if vux("MATRIX").exists() set utblrelmat1.matrix = vux("MATRIX").piece("|",i)
	if vux("MATROW").exists() set utblrelmat1.matrow = vux("MATROW").piece("|",i)
	quit
