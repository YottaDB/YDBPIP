ZCIRCFL(RecordZCIRCULO zcirculo, String vpar, Boolean vparNorm) // ZCIRCULO - Temporary Table for Circulo De Credito Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 08/30/2007 14:22 - joynerd
	// Generated from DATA-QWIK schema in: /profile/v72qa_gtmlx  by: /v72qa_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (4)              03/12/2007
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type public String verrors()

	type String vx(), vxins()                              // audit column array
	type Number %O = zcirculo.getMode()                    // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(zcirculo,.vxins(),1,"|")
	if %O = 1 do AUDIT^UCUTILN(zcirculo,.vx(),1,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("CID").exists()!vx("ACN").exists()!vx("XML").exists()!vx("SEQ").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("ZCIRCULO",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("ZCIRCULO","CID = :zcirculo.cid and ACN = :zcirculo.acn and XML = :zcirculo.xml and SEQ = :zcirculo.seq") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String CID
	type public String ACN
	type public String XML
	type public String SEQ

	type RecordZCIRCULO zcirculo = Db.getRecord("ZCIRCULO","CID=:CID,ACN=:ACN,XML=:XML,SEQ=:SEQ")
	if (%ProcessMode = 2) do {
		do zcirculo.setMode(2)

		do ZCIRCFL(zcirculo,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordZCIRCULO zcirculo

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("ZCIRCULO","CID = :zcirculo.cid and ACN = :zcirculo.acn and XML = :zcirculo.xml and SEQ = :zcirculo.seq") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(zcirculo)) K:$D(vobj(zcirculo,1,1)) ^ZCIRCULO(vobj(zcirculo,-3),vobj(zcirculo,-4),vobj(zcirculo,-5),vobj(zcirculo,-6)) S ^ZCIRCULO(vobj(zcirculo,-3),vobj(zcirculo,-4),vobj(zcirculo,-5),vobj(zcirculo,-6))=vobj(zcirculo)
		#ENDBYPASS
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
			if $D(vobj(zcirculo,1,1)) N vS1,vS2 S vS1=0 F vS2=1:450:$L(vobj(zcirculo,1,1)) S vS1=vS1+1,^ZCIRCULO(vobj(zcirculo,-3),vobj(zcirculo,-4),vobj(zcirculo,-5),vobj(zcirculo,-6),vS1)=$E(vobj(zcirculo,1,1),vS2,vS2+449)
			#ENDBYPASS
	}

	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordZCIRCULO zcirculo


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^ZCIRCULO(vobj(zcirculo,-3),vobj(zcirculo,-4),vobj(zcirculo,-5),vobj(zcirculo,-6))
	#ENDBYPASS
	quit


vreqn // Validate required data items

	type public RecordZCIRCULO zcirculo

	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordZCIRCULO zcirculo

	if zcirculo.cid.isNull() do vreqerr("CID") quit
	if zcirculo.acn.isNull() do vreqerr("ACN") quit
	if zcirculo.xml.isNull() do vreqerr("XML") quit
	if zcirculo.seq.isNull() do vreqerr("SEQ") quit

	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("ZCIRCULO","MSG",1767,"ZCIRCULO."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordZCIRCULO zcirculo

	set X = zcirculo.cid if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("CID", vRM) quit
	set X = zcirculo.acn if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("ACN", vRM) quit
	if zcirculo.xml.length()>12 set vRM = $$^MSG(1076,12) do vdderr("XML", vRM) quit
	set X = zcirculo.seq if 'X.isNull(),X'?1.12N,X'?1"-"1.11N set vRM=$$^MSG(742,"N") do vdderr("SEQ", vRM) quit
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("ZCIRCULO","MSG",979,"ZCIRCULO."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordZCIRCULO zcirculo

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("CID").exists() set vux("CID") = vx("CID")
	if vx("ACN").exists() set vux("ACN") = vx("ACN")
	if vx("XML").exists() set vux("XML") = vx("XML")
	if vx("SEQ").exists() set vux("SEQ") = vx("SEQ")
	do vkey(1) set voldkey = zcirculo.cid_","_zcirculo.acn_","_zcirculo.xml_","_zcirculo.seq // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = zcirculo.cid_","_zcirculo.acn_","_zcirculo.xml_","_zcirculo.seq // Copy new keys into object
	type RecordZCIRCULO vnewrec = zcirculo.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("ZCIRCULO",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordZCIRCULO zcirculo

	if vux("CID").exists() set zcirculo.cid = vux("CID").piece("|",i)
	if vux("ACN").exists() set zcirculo.acn = vux("ACN").piece("|",i)
	if vux("XML").exists() set zcirculo.xml = vux("XML").piece("|",i)
	if vux("SEQ").exists() set zcirculo.seq = vux("SEQ").piece("|",i)
	quit
