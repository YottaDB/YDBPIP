DBTBL5QL(RecordDBTBL5Q dbtbl5q, String vpar, Boolean vparNorm) // DBTBL5Q - QWIK Report Definition Filer
	// Copyright(c)2007 Sanchez Computer Associates, Inc.  All Rights Reserved - 11/09/2007 15:13 - chenardp
	// Generated from DATA-QWIK schema in: /profile/v72framework_gtm  by: /v72framework_gtmlx/crtns/DBSFILB.obj

	// Data Dictionary Data Items (36)             09/20/2006
	/*
		vpar      Runtime qualifiers:      /NOREQ/MECH=REFARR:R

		/[NO]CASDEL   - Cascade delete
		/[NO]FRMFILER - Called from another filer
		/[NO]INDEX    - Update Indexes
		/[NO]JOURNAL  - Journal update
		/[NO]TRIGAFT  - After update triggers
		/[NO]TRIGBEF  - Before update triggers
		/[NO]UPDATE   - Update primary table
		/[NO]VALDD    - Validate column values
		/[NO]VALFK    - Validate foreign keys
		/[NO]VALREQ   - Validate not null values
		/[NO]VALRI    - Validate transaction integrity
		/[NO]VALST    - Validate database state
	*/

	type Public Number ER = 0
	type Public String RM

	type public String verrors()

	catch fERROR {
		if fERROR.type="%PSL-E-DBFILER" do {
			set ER = 1
			set RM = fERROR.description
		}
		else  throw fERROR
	}
	type String vx(), vxins()                              // audit column array
	type Number %O = dbtbl5q.getMode()                     // Processing mode
	set vpar = vpar.get()                                  // Initialize vpar

	if %O = 0 do AUDIT^UCUTILN(dbtbl5q,.vxins(),11,"|")
	if %O = 1 quit:'dbtbl5q.isChanged()  do AUDIT^UCUTILN(dbtbl5q,.vx(),11,"|")

	if 'vparNorm.get() set vpar = $$initPar^UCUTILN(vpar)  // Run-time qualifiers

	if %O = 0 do { quit                                    // Create record control block
		do vinit                                              // Initialize column values
		if vpar["/VALREQ/" do vreqn                           // Check required
		if vpar["/VALDD/" do vddver                           // Check values
		do vexec
	}

	if %O = 1 do { quit                                    // Update record control block
		if vx("LIBS").exists()!vx("QRID").exists() do vkchged quit // Primary key changed
		if vpar["/VALREQ/" do vrequ                           // Check required
		if vpar["/VALDD/" do VDDUX^DBSFILER("DBTBL5Q",.vx)
		set %O = 1 do vexec
	}

	if %O = 2 do { quit                                    // Verify record control block
		if vpar["/VALREQ/" do vreqn                           // Check required
		set vpar = $$setPar^UCUTILN(vpar,"NOJOURNAL/NOUPDATE")
		do vexec
	}

	if %O = 3 do { quit                                    // Delete record control block
		quit:'Db.isDefined("DBTBL5Q","LIBS = :dbtbl5q.libs and QRID = :dbtbl5q.qrid") // No record exists
		do vdelete(0)
	}

	quit

vlegacy(Number %ProcessMode,String vpar) // Legacy interface (^DBSDF9 for %O=0, EXT^DBSFILER for %O = 2)

	type public String LIBS
	type public String QRID

	type RecordDBTBL5Q dbtbl5q = Db.getRecord("DBTBL5Q","LIBS=:LIBS,QRID=:QRID")
	if (%ProcessMode = 2) do {
		do dbtbl5q.setMode(2)

		do DBTBL5QL(dbtbl5q,vpar)
	}

	quit

vLITCHK() quit 0 // Table does not have columns involved in literals


vexec // Execute transaction

	type public Number %O
	type public String vpar,vobj(),vx(),vxins()

	type public RecordDBTBL5Q dbtbl5q

	type String vERRMSG

	if vpar["/VALST/" if '(''Db.isDefined("DBTBL5Q","LIBS = :dbtbl5q.libs and QRID = :dbtbl5q.qrid") = ''%O) set vERRMSG = $$^MSG($select(%O:7932,1:2327)) throw Class.new("Error","%PSL-E-DBFILER,"_vERRMSG.replace(",","~"))

	if vpar'["/NOUPDATE/" do {

		type String n = -1
		type String x

		if %O = 0 for  set n = vobj(dbtbl5q,n).order() quit:n.isNull()  do {
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4),n)=vobj(dbtbl5q,n)
			#ENDBYPASS
		}

		else  for  set n = vobj(dbtbl5q,-100,n).order() quit:n.isNull()  do {
			quit:'$D(vobj(dbtbl5q,n))
			// Allow global reference and M source code
			#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
			#BYPASS
				S ^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4),n)=vobj(dbtbl5q,n)
			#ENDBYPASS
		}

		// Allow global reference and M source code
		#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; GROUP=BYPASS
		#BYPASS
		if $D(vobj(dbtbl5q)) S ^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4))=vobj(dbtbl5q)
		#ENDBYPASS
	}

	quit

vload // Record Load - force loading of unloaded data

	type public RecordDBTBL5Q dbtbl5q
	type String n = ""

	// Allow global reference
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	for  set n=$order(^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4),n)) quit:n=""  if '$D(vobj(dbtbl5q,n)),$D(^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4),n))#2 set vobj(dbtbl5q,n)=^(n)
	#ENDBYPASS
	quit

vdelete(Boolean vkeychg) // Record Delete

	type public String vobj(),vpar
	type public RecordDBTBL5Q dbtbl5q

	if 'vkeychg.get(),dbtbl5q.isChanged() throw Class.new("Error","%PSL-E-DBFILER,Deleted object cannot be modified")


	// Allow global reference - Delete record
	#ACCEPT DATE=04/22/04; PGM=Dan Russell; CR=20602; Group=BYPASS
	#BYPASS
	kill ^DBTBL(vobj(dbtbl5q,-3),6,vobj(dbtbl5q,-4))
	#ENDBYPASS
	quit


vinit // Initialize default values

	type public RecordDBTBL5Q dbtbl5q

	// Type local variables for access keys for defaults
	type public String LIBS
	type public String QRID

	if dbtbl5q.banner.isNull() set dbtbl5q.banner = 1      // banner
	if dbtbl5q.cscmp.isNull() set dbtbl5q.cscmp = 1        // cscmp
	if dbtbl5q.dtl.isNull() set dbtbl5q.dtl = 1            // dtl
	if dbtbl5q.ltd.isNull() set dbtbl5q.ltd = +$P($H,",",1) // ltd
	if dbtbl5q.msql.isNull() set dbtbl5q.msql = 0          // msql
	if dbtbl5q.rsize.isNull() set dbtbl5q.rsize = 80       // rsize
	if dbtbl5q.stat.isNull() set dbtbl5q.stat = 0          // stat
	if dbtbl5q.uid.isNull() set dbtbl5q.uid = $$USERNAM^%ZFUNC // uid
	quit


vreqn // Validate required data items

	type public RecordDBTBL5Q dbtbl5q

	if dbtbl5q.banner.isNull() do vreqerr("BANNER") quit
	if dbtbl5q.cscmp.isNull() do vreqerr("CSCMP") quit
	if dbtbl5q.dtl.isNull() do vreqerr("DTL") quit
	if dbtbl5q.msql.isNull() do vreqerr("MSQL") quit
	if dbtbl5q.stat.isNull() do vreqerr("STAT") quit
	quit

vrequ // Valid required columns on update


	type public String vx()
	type public RecordDBTBL5Q dbtbl5q

	if dbtbl5q.libs.isNull() do vreqerr("LIBS") quit
	if dbtbl5q.qrid.isNull() do vreqerr("QRID") quit

	if 'vobj(dbtbl5q,-100,0,"").order().isNull() do {
		if vx("DTL").exists(),dbtbl5q.dtl.isNull() do vreqerr("DTL") quit
		if vx("STAT").exists(),dbtbl5q.stat.isNull() do vreqerr("STAT") quit
		if vx("BANNER").exists(),dbtbl5q.banner.isNull() do vreqerr("BANNER") quit
		if vx("MSQL").exists(),dbtbl5q.msql.isNull() do vreqerr("MSQL") quit
		if vx("CSCMP").exists(),dbtbl5q.cscmp.isNull() do vreqerr("CSCMP") quit
	}
		if vx("BANNER").exists(),dbtbl5q.banner.isNull() do vreqerr("BANNER") quit
		if vx("CSCMP").exists(),dbtbl5q.cscmp.isNull() do vreqerr("CSCMP") quit
		if vx("DTL").exists(),dbtbl5q.dtl.isNull() do vreqerr("DTL") quit
		if vx("MSQL").exists(),dbtbl5q.msql.isNull() do vreqerr("MSQL") quit
		if vx("STAT").exists(),dbtbl5q.stat.isNull() do vreqerr("STAT") quit
	quit

vreqerr(di) // Required error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DBTBL5Q","MSG",1767,"DBTBL5Q."_di)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit




vddver // Validate data dictionary attributes

	type public Number %O
	type public String vpar,vx()

	type String vRM,X
	type public RecordDBTBL5Q dbtbl5q

	if (%O = 2) do vload

	if vobj(dbtbl5q,0).exists() do {

		if '("01"[dbtbl5q.BANNER) set vRM=$$^MSG(742,"L") do vdderr("BANNER", vRM) quit
		if dbtbl5q.breakon.length()>60 set vRM = $$^MSG(1076,60) do vdderr("BREAKON", vRM) quit
		set X = dbtbl5q.cntmatch if 'X.isNull(),X'?1.8N,X'?1"-"1.7N set vRM=$$^MSG(742,"N") do vdderr("CNTMATCH", vRM) quit
		set X = dbtbl5q.cntrec if 'X.isNull(),X'?1.8N,X'?1"-"1.7N set vRM=$$^MSG(742,"N") do vdderr("CNTREC", vRM) quit
		if '("01"[dbtbl5q.CSCMP) set vRM=$$^MSG(742,"L") do vdderr("CSCMP", vRM) quit
		if '("01"[dbtbl5q.DTL) set vRM=$$^MSG(742,"L") do vdderr("DTL", vRM) quit
		set X = dbtbl5q.labopt if 'X.isNull(),X'?1.2N,X'?1"-"1.1N set vRM=$$^MSG(742,"N") do vdderr("LABOPT", vRM) quit
		set X = dbtbl5q.ltd if 'X.isNull(),X'?1.5N set vRM=$$^MSG(742,"D") do vdderr("LTD", vRM) quit
		if '("01"[dbtbl5q.MSQL) set vRM=$$^MSG(742,"L") do vdderr("MSQL", vRM) quit
		if dbtbl5q.orderby.length()>100 set vRM = $$^MSG(1076,100) do vdderr("ORDERBY", vRM) quit
		if dbtbl5q.pfid.length()>60 set vRM = $$^MSG(1076,60) do vdderr("PFID", vRM) quit
		if dbtbl5q.pgm.length()>8 set vRM = $$^MSG(1076,8) do vdderr("PGM", vRM) quit
		if dbtbl5q.reptype.length()>6 set vRM = $$^MSG(1076,6) do vdderr("REPTYPE", vRM) quit
		set X = dbtbl5q.rsize if 'X.isNull(),X'?1.4N,X'?1"-"1.3N set vRM=$$^MSG(742,"N") do vdderr("RSIZE", vRM) quit
		if '("01"[dbtbl5q.STAT) set vRM=$$^MSG(742,"L") do vdderr("STAT", vRM) quit
		if dbtbl5q.uid.length()>20 set vRM = $$^MSG(1076,20) do vdderr("UID", vRM) quit
	}

	if vobj(dbtbl5q,1).exists() do {

		if dbtbl5q.qid1.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID1", vRM) quit
	}

	if vobj(dbtbl5q,2).exists() do {

		if dbtbl5q.qid2.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID2", vRM) quit
	}

	if vobj(dbtbl5q,3).exists() do {

		if dbtbl5q.qid3.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID3", vRM) quit
	}

	if vobj(dbtbl5q,4).exists() do {

		if dbtbl5q.qid4.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID4", vRM) quit
	}

	if vobj(dbtbl5q,5).exists() do {

		if dbtbl5q.qid5.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID5", vRM) quit
	}

	if vobj(dbtbl5q,6).exists() do {

		if dbtbl5q.qid6.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID6", vRM) quit
	}

	if vobj(dbtbl5q,7).exists() do {

		if dbtbl5q.qid7.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID7", vRM) quit
	}

	if vobj(dbtbl5q,8).exists() do {

		if dbtbl5q.qid8.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID8", vRM) quit
	}

	if vobj(dbtbl5q,9).exists() do {

		if dbtbl5q.qid9.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID9", vRM) quit
	}

	if vobj(dbtbl5q,10).exists() do {

		if dbtbl5q.qid10.length()>78 set vRM = $$^MSG(1076,78) do vdderr("QID10", vRM) quit
	}

	if vobj(dbtbl5q,11).exists() do {

		set X = dbtbl5q.trans if 'X.isNull(),'Db.isDefined("STBLTFMT","X") set vRM = $$^MSG(1485,X) do vdderr("TRANS", vRM) quit
	}

	if vobj(dbtbl5q,12).exists() do {

		if dbtbl5q.fld1.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD1", vRM) quit
	}

	if vobj(dbtbl5q,13).exists() do {

		if dbtbl5q.fld2.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD2", vRM) quit
	}

	if vobj(dbtbl5q,14).exists() do {

		if dbtbl5q.fld3.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD3", vRM) quit
	}

	if vobj(dbtbl5q,15).exists() do {

		if dbtbl5q.fld4.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD4", vRM) quit
	}

	if vobj(dbtbl5q,16).exists() do {

		if dbtbl5q.fld5.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD5", vRM) quit
	}

	if vobj(dbtbl5q,17).exists() do {

		if dbtbl5q.fld6.length()>78 set vRM = $$^MSG(1076,78) do vdderr("FLD6", vRM) quit
	}
	if dbtbl5q.libs.length()>10 set vRM = $$^MSG(1076,10) do vdderr("LIBS", vRM) quit
	if dbtbl5q.qrid.length()>12 set vRM = $$^MSG(1076,12) do vdderr("QRID", vRM) quit

	if vobj(dbtbl5q).exists() ! 'vobj(dbtbl5q,"").order().isNull() do {

		if dbtbl5q.desc.length()>40 set vRM = $$^MSG(1076,40) do vdderr("DESC", vRM) quit
	}
	quit

vdderr(di, vRM) // Column attribute error

	type public Boolean ER = 0
	type public String RM
	do SETERR^DBSEXECU("DBTBL5Q","MSG",979,"DBTBL5Q."_di_" "_vRM)
	if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~"))
	quit


vkchged // Access key changed

	type public RecordDBTBL5Q dbtbl5q

	type public Boolean ER = 0
	type public String RM,vpar,vx()

	type Number %O = 1
	type String vnewkey,voldkey,vux

	if vx("LIBS").exists() set vux("LIBS") = vx("LIBS")
	if vx("QRID").exists() set vux("QRID") = vx("QRID")
	do vkey(1) set voldkey = dbtbl5q.libs_","_dbtbl5q.qrid // Copy old keys into object

	set vpar = $$setPar^UCUTILN(vpar,"NOINDEX")            // Switch Index off
	do vload                                               // Make sure all data is loaded locally
	if vpar["/VALREQ/" do vrequ
	if vpar["/VALDD/" do vddver
	do vexec

	do vkey(2) set vnewkey = dbtbl5q.libs_","_dbtbl5q.qrid // Copy new keys into object
	type RecordDBTBL5Q vnewrec = dbtbl5q.copy()
	do vnewrec.setMode(0)
	do vnewrec.save("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")

	set %O = 1 do CASUPD^DBSEXECU("DBTBL5Q",voldkey,vnewkey) if ER throw Class.new("Error","%PSL-E-DBFILER,"_RM.get().replace(",","~")) // Cascade update

	do vkey(1)                                             // Reset key for delete
	set vpar = $$initPar^UCUTILN("/NOVAL/NOCASDEL/NOJOURNAL/NOTRIGBEF/NOTRIGAFT/")
	set %O = 3 do vdelete(1)                               // Delete old record

	quit

vkey(Number i) // Restore access keys

	type public String vux()
	type public RecordDBTBL5Q dbtbl5q

	if vux("LIBS").exists() set dbtbl5q.libs = vux("LIBS").piece("|",i)
	if vux("QRID").exists() set dbtbl5q.qrid = vux("QRID").piece("|",i)
	quit
